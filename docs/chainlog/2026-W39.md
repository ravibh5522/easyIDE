# Chainlog — 2026-W39

## 2026-09-23 22:32

- Added `docs/ux-overhaul/{arch.md,tracker.md}` and registered it in `docs/README.md`: consolidated output of five read-only audits (performance, visual design, layout/navigation, settings, feature gaps) with file:line findings, a phased roadmap, and four ADRs still to write (visual identity, editor engine, settings schema/theme import, workspace session lifetime).
- Why: user reported the whole app feels dull, slow and laggy. Two data-loss bugs are flagged as P0: Back kills ptys and dirty buffers, and the dirty dot closes a tab without asking.

## 2026-09-23 22:59

- Sped up syntax highlighting first paint (`workspace/syntax/*`, `EditorPane.kt`, `WorkspaceViewModel.kt`):
  - `ScopeRules` now memoises each scope's colour role, replacing a scan of about 40 rules that allocated a string per token.
  - The 120 ms debounce now applies only to real edits, not to opening a file, switching tabs, scrolling or theme changes.
  - Tokenizer state is kept for the 8 most recent tabs (it was the active tab only, so every switch re-tokenized from line 0).
  - Grammars for root-level files are prewarmed on a background thread when the tree loads, so the regex compile happens off the first-open path.
  - Superseded passes stop between lines instead of holding the lock.
- Did not add a SQLite or disk token cache. TextMate `StateStack` cannot be serialised, Room is already ruled out ([0005](../decision/0005-sandbox-environment-sharing-model.md)), and the cold cost is regex compilation, which prewarm addresses.
- Verified: `./gradlew :app:assembleDebug` passes. No device was attached, so the speedup has not been measured.

## 2026-09-23 23:38

- Added the proposed design `docs/extension-sdk/` (`arch.md`, `sdk-reference.md`, `tracker.md`) and registered it in `docs/README.md`. It covers:
  - VS Code-level language features: language-server client, plus features that need no server;
  - our own extension SDK: a `.easyext` package and a manifest shaped like VS Code's with an `easyide` block;
  - a declarative action list, and a WASM logic layer whose capability limits are enforced;
  - a static signed registry with no backend;
  - a customization model where everything can be overridden;
  - milestones M0-M8.
- Added a pointer from `docs/extensions/arch.md` to the new design.
- Licenses checked against primary sources on 2026-09-23 are recorded in `arch.md` section 8. Five ADRs are proposed and not yet written: E (SDK/manifest), F (WASM layer), G (SDK licensing), H (registry/signing), I (LSP client).

## 2026-09-24 00:19

- Made the `docs/extension-sdk/` set (arch, sdk-reference, HLD, features, threat model, rules, test plan, author guide, eight LLDs) and ADRs 0013-0017 agree with each other:
  - arch.md now uses the sdk-reference keys and defaults: LSP budget 1200/400 MB, `lsp.maxServers`, `lsp.restart.*`, WASM 64 MB / 2000 ms, the crash window, utf-16 positions, and one session per `(env, project, serverId)`.
  - `lsp.maxSyncBytes`, `memorySampleSec` and `logBufferLines` became `LspPolicy` constants. Protocol and safety limits that no settings layer may relax are listed in a new sdk-reference "Fixed limits and protected keys" section, and rules R-ENG-08 now allows that.
  - sdk-reference now includes the refinements the LLDs made: the install layout (`<id>/<version>` plus `current`), WASM providers and the `request` message, registry `entries/`, integer-only signed JSON, anti-rollback, the extra CLI flags and `registry build`.
  - Threat-model mitigations M-11, M-12, M-22 and M-04, the `net.fetch` private-address block and the WASM re-entrancy refusal are now adopted by their owner LLDs.
  - One name for each concept: `ActionRunner` everywhere, and user enablement lives in `extensions.disabled` rather than `state.json`. The test scenario format is defined in `lld/cli.md`.
  - arch.md sec 9 no longer says the Claude key is unreadable by every layer. That is true only for in-app layers: sandbox processes can read the CLI's credentials.
- Added a document map to arch.md and ADR links to hld.md and tracker.md. The Deviations sections are reduced to "resolved" notes.
- Verified: a link, anchor, ASCII and line-count check over the 24 files finds no broken links, no non-ASCII text, and no file over 599 lines.

## 2026-09-24 01:07

- Implemented ux-overhaul Phase 0 and extension-sdk M0/M1 foundations with five parallel agents, one local worktree each, merged here by the head agent. The trackers have the per-item status.
  - **Safety:**
    - S1: an unsaved-changes guard on Back and on the rail.
    - S2: a dirty tab asks Save / Discard / Cancel on close, with a 48dp target.
    - S3/PS7: a "Project not found" state, and no second HomeViewModel.
    - S4: IME insets plus `adjustResize`.
    - S5: `smallestScreenSize` in configChanges.
  - **Sandbox and git:**
    - PF5: environment file I/O moved off the main thread.
    - PF6: `WorkspaceGitController` refreshes git one run at a time, with a cached JGit repository.
    - PS5: the tree re-lists once and emits one update.
    - PF10: install-log lines are batched.
    - PS8: the terminal uses a blocking read instead of polling.
    - WorkspaceViewModel is down from 641 to 566 lines.
  - **Build:**
    - PF1: R8 and shrinkResources on release and canary.
    - PF2: profileinstaller and a `:baselineprofile` generator. No profile yet; it needs a device.
    - PF8: a splash screen with light/night window themes.
    - PF9: the grammar index loads lazily.
  - **Platform:**
    - A schema-driven `SettingsStore` (theme, editor font size and line height, terminal font size) with generated, searchable settings rows.
    - A command registry and keymap with hardware shortcuts that respect terminal focus, and a command palette on Ctrl+Shift+P.
  - **Editor:**
    - PF3/PF4: the line window only changes once scrolling settles.
    - PF7: the terminal stays composed when hidden.
    - PS3: line counts and dirty checks are cached.
    - NS: `language-configuration.json` from VS Code 1.139.0 (MIT) for 57 languages, with a generic fallback for the rest. Covers auto-close, overtype, surround, Enter/indent rules, comment toggle and matching bracket.
  - Tests: JUnit 4.13.2, test-only. 53 JVM tests.
- Merge fix: `LanguageConfigs` now triggers the lazy grammar-index load itself, and the two locks are always taken in the same order.
- Verified: `./gradlew :app:assembleDebug :app:testDebugUnitTest :app:assembleRelease :app:assembleCanary :baselineprofile:assemble` passes. No device or emulator was available, so nothing was run.

## 2026-09-24 01:45

- sandbox-runtime: new `download/` (`Sha256`, `DownloadRequest`/`DownloadEvent`/`DownloadError`, `VerifiedDownloader`: streaming SHA-256, `.part` + HTTP Range resume, size cap, cancellable Flow); `RootfsProvisioner` now downloads through it and re-hashes cached images; `SandboxImage.urlByAbi` -> `rootfsByAbi: Map<String, RootfsArchive(url, sha256)>` with ubuntu-base 24.04.3 digests pinned in `app/.../SandboxImages.kt`. Extension storage: `SandboxPaths` extension dirs/state file/staging, `extensions/ExtensionNames.kt` (`ExtensionId`, `ExtensionVersion`), `GuestBind`/`GuestBindSource` + `LaunchRequest.extraBinds` in both launchers, `EnvironmentExtensionBinds` wired into `LinuxEnvironment.start`/`interactiveShellParams`. First JVM unit tests for the module (38).
- Why: closes the "rootfs has no checksum" gap in sandbox-runtime/tracker.md and lays the storage/guest-visibility prerequisites from extension-sdk/lld/registry-and-install.md sec 11; one downloader for rootfs and future extension packages.

## 2026-09-24 01:50

- Wrote [decision 0018](../decision/0018-editor-engine-and-decorations.md), ux-overhaul ADR-B. The app keeps `BasicTextField` for now, adds decoration layers that do not depend on the editor engine, and builds its own line-virtualised Compose editor for PE1. Licences and maintenance for sora-editor (LGPL-2.1), CodeView, KodeEditor and compose-code-editor (all MIT) were checked on GitHub today. All four were rejected. Linked from ux-overhaul arch/tracker, extension-sdk arch 7.9/tracker, and the decision index.
- Added `workspace/decor/`:
  - `DecorationModel` / `DecorationSet`: pure-Kotlin layers in UTF-16 offsets. The layers are diagnostics, document highlights, search matches, inlay hints, gutter markers and code lenses. Each layer has a priority, a drop-or-clip edit policy and per-source entries. Every write names the text its offsets refer to. `OffsetEdit` derives the edit from the prefix/suffix diff and never splits a surrogate pair.
  - Draw-phase painters: squiggle waves (hints are three dots), background ranges, end-of-line inlay ghost text and one gutter glyph per line. Painting is limited to the live line window, and the text sits in its own graphics layer, so a decoration change triggers no recomposition and no relayout.
  - `EditorGeometry` maps offsets to viewport rectangles.
  - `EditorPopup` stays inside the viewport. Escape and Back dismiss it, and it leaves focus in the text field.
- New `DecorationColors` tokens under `EditorColors.decorations`, taken from VS Code's registered defaults (checked in its source today). A `DecorationRegistry` in `WorkspaceViewModel` is dropped on tab close and moved on rename.
- Measured on a desktop JVM: a 256 KB buffer with 5,000 decorations costs about 1 ms per keystroke to diff and shift. A paint-window query costs 5 us.
- Verified: `./gradlew :app:testDebugUnitTest :app:assembleDebug` passes, with 41 new JVM tests. No device was used, so the rendering has not been seen on screen.

## 2026-09-24 02:23

- Added the `:extensions` runtime core, a pure JVM module (`services/mobile/extensions`). It covers:
  - the manifest JSON Schema and a subset validator, with the schema in `services/shared/extension-schema` under Apache-2.0 per [0015](../decision/0015-extension-sdk-licensing-apache.md);
  - the manifest parser and SemVer ranges;
  - a `ContributionRegistry` with 23 typed stores;
  - the when-clause parser and evaluator, plus `ContextKeyService`;
  - `ActionRunner` for the full action vocabulary through a `HostPort`, with injection-safe quoting;
  - capability checks, activation, the crash journal and safe mode, and enablement.
  - 123 JUnit tests.
- NOTICE now records kotlinx-serialization and the VS Code language-configuration files.
- Verified: `./gradlew :extensions:test :app:assembleDebug`.

## 2026-09-24 02:29
- lsp: first real code in `:lsp` (pure JVM, no new dependency; JSON via kotlinx-serialization tree API). JSON-RPC 2.0 over `Content-Length` framing (`FrameReader`/`FrameWriter` with header, size and depth limits from `LspPolicy`; `JsonRpcConnection` with single writer, id correlation, `$/cancelRequest` both ways, per-method timeouts); protocol types and total parsers for every M2/M4 feature; `ClientCapabilitiesBuilder` per milestone and UI layer; `LineIndex` (UTF-16), prefix/suffix `TextDiff` with `EditDelta`; `WorkspacePathMapper` + `FileUri` (vscode-uri-compatible encoding); `DocumentStore` / `DocumentSync` (incremental and Full, size cap, save, reopen detection); `LspSession` driven by a pure `SessionStateMachine` (lsp-lifecycle.md 1.2 table) with crash backoff, idle shutdown, pull diagnostics; `LanguageServerManager` (lazy start, admission, RSS sampler through `MemoryProbe`, `MemoryPolicy` kill order, memory pressure); `LspClient` facade with merge / first-non-empty / single-owner routing, resolve, semantic-token delta cache, `WorkspaceEditApplier`. 114 JVM tests over an in-process fake server speaking real framed JSON-RPC.
- sandbox-runtime: `shell/ServerProcessFactory.kt` (new) spawns servers on three plain pipes via `/bin/sh -c 'exec "$0" "$@"'`; hooks `SandboxShell.startPiped` and `LinuxEnvironment.startPiped` (same proot preparation and extension binds as terminals, no Android-shell fallback).
- Why: M2 LSP core per extension-sdk/lld/lsp-client.md, lsp-lifecycle.md, lsp-features.md and decision 0017; the app wiring (ports, bridge, UI) is the next wave.

## 2026-09-24 04:10
- app: wired `:lsp` into the app (LSP-06..11, 20..32 client side).
  - `app/lsp/`: `LspRuntime` (composition root piece in `AppContainer`), `SandboxServerLauncher` over `ServerProcessFactory`, `ProcTree`/`ProcMemoryProbe` (Android has no `Process.pid()`: the server's proot is found in `/proc` as the app's child whose argv ends with `ServerProcessFactory.guestArgv` and binds the project dir; RSS = VmRSS of it and its descendants), `SettingsConfigurationProvider` (`workspace/configuration` from settings resolution per project), `LspMessageBus` (`showMessage` snackbars, `showMessageRequest` dialogs), `EditPortRegistry` (open buffers while a workspace is open, disk otherwise), `ServerRegistry` + `ServerConfigMerge` (`lsp.servers` layered over registered `ServerDeclarationProvider`s - the extension runtime's hook). `EasyIdeApplication.onTrimMemory` forwards to the manager.
  - `workspace/lsp/`: `WorkspaceLspController` (the editor's new `EditorInteraction`), documents from tabs, diagnostics presenter + Problems/References/Outline right stage, completion + snippet tab stops, hover/signature, navigation/rename, code actions/formatting/save participants, highlights/inlay hints, status item, install notice, `@`/`#` symbol picker, 17 commands with keybindings (two-step chords via `ChordDispatcher`).
  - `:lsp`: `ClientUi.withheld` so folding, selection ranges and document links (no presenter yet) are not advertised.
  - Settings: 30 `lsp.*`/`editor.*` keys in `LspSettingsSchema`, `Setting.Json` for object values, read per project and language.
- Why: the M2 LSP UI of lsp-features.md on the ADR-B decoration layers.
- Verified: see the implementation report; no device run (the phone was reserved).


## 2026-09-24 10:43

- Wired `:lsp` into `:app` (`app/lsp/`, `workspace/lsp/`):
  - sandbox server launcher; a `/proc` memory probe; trim-memory mapping.
  - `lsp.servers` resolved through the trust-gated settings snapshot.
  - Status bar item, Problems and References panels, and the Outline panel.
  - Squiggles, gutter markers, occurrence highlights and inlay hints on the decoration layer.
  - Completion with resolve and snippet tab stops, hover, signature help, definition, rename with preview, and quick fixes.
  - Formatting, including on save and `codeActionsOnSave`, plus the `@` and `#` symbol pickers.
  - An install notice for missing servers, and 17 keybound commands.
- Fixed a race in `LspSession`: the state became Running before server capabilities were published, so a request sent right away found no capable server. It was caught as an intermittent `LspClientTest` failure under load.
- Verified: all module tests plus `:app:assembleDebug :app:assembleRelease`. Nothing has been exercised against a real server on a device yet.

## 2026-09-24 11:15

- Merged the extension integration (`app/extensions/`, `workspace/ext/`, Extensions screen, built-in packs) onto the LSP UI integration. Resolved conflicts in SettingsSchema, ViewModelFactory, AppNavHost, Command, CommandPalette, Keymap, WorkspaceCommands, WorkspaceChrome (StatusBar: `leading`/`trailing` extension items plus the LSP `extra`), EditorPane (hoisted `EditorSelections` plus `EditorInteraction`; LSP selection requests now write the hoisted caret; gutter caret line reads the hoisted state), WorkspaceViewModel (terminals via `WorkspaceTerminals`, which now uses `InstallLogPump` and takes the LSP install `initialCommand`), WorkspaceScreen (explorer panes split into `WorkspacePanes.kt` to stay under 600 lines).
- Keymap: one `KeyBinding` with `prefix`, `whenExpr` and `args`; `ChordDispatcher` checks `when` at both steps and passes args; `KeyNames.parseSequence` lets keybindings.json and contributed keybindings declare two-press chords (three or more are still reported).
- Seams: `AppHostPort.lspRequest` -> `WorkspaceBridge.lspRequest` -> `LspRequestGateway` (allowlisted methods, best ready server offering the feature, caret params by default, `then` applied); contributed `languageServers` -> `ContributedServers.Provider` registered on `ServerRegistry` (own environment, `lsp.spawn` granted, `${extensionPath}`/`${config:}` expanded, `sandbox.install` as install recipe); `lspReady:`/`lspState:`/`lspSupports:<lang>:<feature>` fed from manager statuses into `WorkspaceContextFeed`. `:lsp` gained `LspMethods.ALL`.
- Why: both waves asked for these seams; key names follow sdk-reference (per language, not per server).
- Verified: `:lsp:test :extensions:test :ext-wasm:test :sandbox-runtime:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease`. No device run.

## 2026-09-24 11:45

- Added five declarative built-in packs (`app/src/main/assets/extensions/`): `easyide.tablet-toolbar` (touch toolbar and editor-title entries for built-in edit/LSP commands), `easyide.key-rows` (Symbols and Navigation rows chosen via `keyRows.active`, a Shell row for the terminal behind its own setting), `easyide.toggles` (`toggleConfig` commands for honoured `editor.*`/`terminal.*`/`appearance.*` keys, status items reflecting format-on-save and inlay hints), `easyide.git-extras` (branches, stash, graph, file history, blame, pull strategy, confirmed push/publish/force-with-lease), `easyide.project-tasks` (make, npm, pytest, cargo; buttons keyed to the open file).
- App: `editor.action.commentLine` (Ctrl+/) over the existing `EditCommands.toggleComment`; `BuiltInCommandTable` registers every `CommandIds.ALL` id as a built-in contribution (LSP commands were missing, so pack menus naming them were dropped); menu entries bound to a built-in are greyed while the app command is disabled; workspace contributions rebuild on settings changes so `config.*` clauses and status items follow toggles.
- Fixed `easyide.git-commands` commit prompt: `validate` is a full match, so `\S` accepted only one-character messages.
- Why: tablet productivity through the extension surface instead of hardcoded UI; no new context keys or SDK fields.
- Verified: `:app:testDebugUnitTest` (pack reference and behaviour tests), `:app:assembleDebug`. No device run.


## 2026-09-24 13:56

- Moved the extension validation core out of `services/mobile/extensions` into the new Apache-2.0 `:extension-schema` module at `services/shared/extension-schema/src/main/kotlin`: the JSON helpers, the schema validator, the manifest parser and decoders, the when-clause parser, capability rules, the action/contribution data model, package layout, settings key definitions and `ExtensionPolicy`/`AppApi`. Package names are unchanged, and `:extensions` depends on the new module with `api`. The runtime stays under the app licence. The moved `ZipSymlinks` now throws `ZipFormatException`, and `PackageUnpacker` maps it to `PackageRefused`.
- Added RFC 8785 `Jcs`, `Sig`/`KeyIds`/`SignedBytes`, `SignatureVerifier` behind an `Ed25519` port, and `JdkEd25519` (`dev.easyide.extensions.registry`), plus `builtin-commands.json`/`BuiltInCommands` with an app contract test against `CommandIds.ALL`.
- Added `tools/easyide-ext` (Apache-2.0, its own Gradle build that compiles the shared core). Commands: `init` (four templates), `validate`, deterministic `package`, `keygen` (PBES2-encrypted, `--rotate`), `sign` and `verify`, with `--json` output and exit codes 0/1/2.
- `PackageLayoutReader.read` takes an optional `exclude` predicate. `StepExecutor` uses a safe call where the smart cast across modules no longer compiles.
- Why: M5 CLI, and M6 signature building blocks. The owner chose to relicense the validation core so the CLI reuses the app's parser; see the [0015 amendment](../decision/0015-extension-sdk-licensing-apache.md).
- Verified: `:extension-schema:test :extensions:test :ext-wasm:test :app:testDebugUnitTest` (a clean app rebuild), and `tools/easyide-ext` `./gradlew test` (17 tests). Also ran each command by hand against scratch extensions. `:lsp:test` has one failure that was already there before this work: `LspClientTest.requestFlushesTheDocumentFirstAndIsStampedWithItsVersion`, where didChange is never recorded before hover, and it fails every run in this container. Not touched. No device run.

## 2026-09-24 15:32

- Home rebuilt (`ui/screens/home/`): list-detail at expanded width, one column with a detail screen otherwise; rich project cards (hash-tinted monogram, location, relative last-opened, git branch and dirty count, language dot, environment badge), search and sort, long-press / secondary-click menu (rename, duplicate, delete with confirm, change environment, open folder location), SAF folder import, https clone, skeleton, empty state, Install Linux prompt. Per-project facts load off the main thread through the new `GitService.summary` (branch + change count without caching a JGit handle), a language guess from root files and `RecentFiles.scan` (newest files by mtime), cached with a freshness rule.
- `:sandbox-runtime`: `ProjectManager.rename/duplicate` and `create(seedStarterFiles)`; `delete` and duplicate use the new symlink-safe `SafeTree` so a link in a project can never delete or copy another environment's files; shared `ProjectNames` rules; `InstallEvent` structured progress on `LinuxEnvironment.install` / `RootfsProvisioner.provision` (download, extraction and setup steps), cancellable extraction that wipes a half-unpacked rootfs.
- Onboarding is now Welcome, battery exemption, notifications (API 33+), first environment install with progress/cancel/resume/retry, Done, and completes only at the end; the same install UI is the `install_linux` route opened from Home. NewProject: tokens instead of literals, sandbox enums through string resources, typed errors. Settings added to the workspace rail; "Open with terminal" workspace route argument. New `strings_home.xml`; ADR [0021](../decision/0021-onboarding-permissions-and-install-keepalive.md).
- Why: ux-overhaul Pillar 4 Home/onboarding and Pillar 6 Projects/Sandbox UI items.
- Verified: `:sandbox-runtime:testDebugUnitTest :app:testDebugUnitTest :app:assembleDebug`. No device run: battery/notification prompts, the foreground service during an install, SAF import and the real clone were not exercised.

## 2026-09-24 15:10
- M3 Python pack: added `app/src/main/assets/extensions/easyide.python/`. It contains the grammar, language configuration and Python snippets, moved with `git mv` from `assets/grammars/` and `easyide.core-snippets`. It also adds pyright and ruff `languageServers` with a `sandbox` recipe, `python.runFile` (Ctrl+Shift+R, touch toolbar, editor title), `python.selectVenv` and a `python.interpreter` setting and status item. `grammars/index.json` drops `source.python` and its 23 extension and 14 filename mappings, which the pack's `languages` entry now claims. `tools/build-grammars.py` hands owned scopes to their pack (`PACK_GRAMMARS`).
- `ContributedServers`: built-in packs declare their servers in every environment. Before this, they were global and so never matched an environment.
- `LocalInstaller` no longer refuses packs with `sandbox.install`. The capability sheet lists the setup commands and server commands verbatim, and the steps run later in a visible terminal from the missing-server notice.
- Tests: new `PythonPackTest`, which checks that the `.easyext` install matches the built-in copy (descriptor, servers, recipe, environment scoping). Updated pack list, toolbar and task-button expectations, and install-flow expectations for staged steps.
- Why: M3 exit, "Python pack removed from app code and reinstalled as .easyext with identical behaviour" (arch.md sec 12).
- Verified: `:app:testDebugUnitTest`. No device run, and pyright and ruff have not been started against a real environment.
## 2026-09-24 14:30
- Added rollback for locally installed extensions: `LocalInstaller.rollback(id, scope, envId, approve)` re-validates the retained previous version with the install pipeline (layout, `ManifestParser`, sandbox-steps and built-in refusals, plus id/version/scope must match the dir), flips `current` atomically, records the target as current in `state.json` and keeps the version rolled back from as the new previous, so a second rollback rolls forward. Returns `Done`, `NeedsApproval` (capabilities never approved for that version; nothing changes) or `Refused` (nothing retained, built-in, not installed, invalid, revoked, I/O failure).
- `state.json` install entries gain an optional `previous{version, approvedCapabilities}`; files without it still load, and rollback then falls back to the single retained version dir with no approvals. If the process dies between the flip and the state write, the active version lists as unapproved and "back" is the recorded version with its approvals. Revocation is an injected `isRevoked` predicate (always false until M6).
- Extensions screen: "Roll back to <version>" in the detail view when a version is retained, a confirmation dialog, and the capability sheet when approval is needed. The inventory publishes the retained versions; the rescan after the flip reloads the extension.
- Why: M3 exit criterion "disable/uninstall/rollback work" (registry-and-install.md sec 10).
- Verified: `:app:testDebugUnitTest` (301 tests, 6 new in `RollbackTest`), `:app:assembleDebug`. No device run.
- Extension colour themes are now selectable and applied. `workbench.colorTheme` (G, new in `SettingsSchema`, edited only through the picker) holds a theme label or `<extId>/<themeId>`. `ContributedThemeCatalog` reads the file on IO (`ColorThemeFile`: JSONC, `include` relative to the including file, may climb but not leave the package, cycle and depth bounds, a `.tmTheme` `tokenColors` path is skipped with a warning), maps it with `VsCodeThemeMapper` over the `uiTheme` base (vs -> Paper light, vs-dark -> Graphite dark, hc-black/hc-light -> the high-contrast pair), and caches the result per file version. `ExtensionsContainer.colorTheme` feeds `EasyIdeTheme(contributed = ...)`, so the Material scheme and editor/syntax colours come from one set of tokens. An unknown, disabled or uninstalled theme, or one that fails to load, falls back to `appearance.themeMode`. Failures go to the Extension Log once.
- The theme picker lists extension themes as preview cards after the built-in modes. Tapping a built-in card clears `workbench.colorTheme`, and reset clears both keys.
- New built-in pack `easyide.themes` (Apache-2.0) with two original themes, Ember Night (dark) and Morning Linen (light). `BuiltInPaletteContrastTest` holds them to the same WCAG thresholds as the built-in palettes.
- Not done: `workbench.colorCustomizations` and the `editor.*ColorCustomizations` layers (design 8.3), `semanticHighlighting`, font styles, and icon themes. A theme selected at launch paints after the extension inventory loads, so the built-in palette can show for a moment first. The toggles pack's theme-mode cycle has no visible effect while an extension theme is selected.
- Verified: `:app:testDebugUnitTest` (310 tests, 0 failures), `:app:assembleDebug`, and `tools/easyide-ext` `./gradlew test`. No device run.
## 2026-09-24 16:05
- M4 language packs (built-in, Apache-2.0): `easyide.typescript` (typescript-language-server 4 over TypeScript 5, run with Node), `easyide.go` (gopls, run/test package), `easyide.cpp` (clangd), `easyide.shell` (bash-language-server 5.6 with ShellCheck), `easyide.web` (HTML, CSS/SCSS/Less and JSON servers from vscode-langservers-extracted 4), `easyide.yaml` (yaml-language-server 1), `easyide.markdown` (VS Code's Markdown server) and `easyide.rust` (rust-analyzer through rustup). Each pack gives setup steps, a verify step, uninstall steps and a network capability, and none bundles a binary. The versions are pinned to what Ubuntu 24.04's Node 18.19 can run: typescript-language-server 5 or later and bash-language-server 5.7 or later need Node 20+, and TypeScript 7 ships no tsserver. Licences were checked on the npm registry on 2026-09-24: typescript-language-server and typescript are Apache-2.0; bash-language-server, yaml-language-server, vscode-langservers-extracted and pyright are MIT. gopls (0.18) and clangd (18) come from Ubuntu noble apt.
- `:lsp` `LanguageIds.wire`: `didOpen` sends `typescriptreact`/`javascriptreact` for the editor's `tsx`/`jsx`, as servers expect.
- Tests: `LanguagePacksTest` checks that the M4 language set is covered, that servers attach to detectable languages, that every pack has a setup, verify and network path, and that built-ins declare their servers and recipes in any environment. `LanguageIdsTest`. Pack list updated.
- Verified: `:lsp:test :app:testDebugUnitTest`. No server has been launched in a real environment yet.
## 2026-09-24 16:50
- M6 shared core (`services/shared/extension-schema`, `dev.easyide.extensions.registry`): added `IndexModel` (strict parsing of `index.json` entries, `publishers/<p>.json` and `revocations.json`), `RegistryVerifier` and `Catalog`, plus `RegistrySigning` for producing documents.
  - `RegistryVerifier` covers sdk-reference client steps 1-4: root signatures, anti-rollback, the publisher file, the entry signature with an active and unrevoked key, the TOFU pin and rotation chain, revoked ranges, and size and sha256.
  - `Catalog` provides the latest compatible, unrevoked version, search and filters.
  - 9 new tests.
- M5/M6 CLI: `easyide-ext publish` signs the entry and commits it (with the package when `--package-base` is given) to a branch through the system git, argv only. `easyide-ext registry build` verifies every entry and root-signs the index, publishers and revocations. `RegistryFlowTest` runs the whole flow against a local bare repo.
- Verified: `:extension-schema:test :extensions:test` and `tools/easyide-ext` `test` (19 tests).
## 2026-09-24 17:40
- 0015 second amendment, at the owner's direction: moved `ActionRunner`, `StepExecutor`, `VariableResolver`, `ActionContext`, `HostPort`, `WhenEvaluator` and `ContextKeyService` from `:extensions` to Apache-2.0 `services/shared/extension-schema`. Also moved `ModuleValidator` (now public), `Meter`, `InstructionScanner`, `WasmBytes`, `WasmLimits`, `WasmPolicy` and `Errors` there from `:ext-wasm`. Package names are unchanged.
  - The shared core now depends on Chicory's parser (`chicory-wasm`); the interpreter stays in `:ext-wasm`, which now depends on `:extension-schema`.
  - Tests of the moved code went with it. `TestSupport`, `FakeHost` and the example packages are now `:extension-schema` test fixtures, and `:extensions` tests consume them.
  - `StepExecutor` is unchanged apart from the earlier smart-cast fix.
- New `WasmStaticCheck` runs size, metering, parse and ABI v1 validation, the same steps as `WasmModuleLoader`.
- CLI: `validate` runs the full WASM check. New `easyide-ext test` replays `test/*.json` scenarios with the real `ActionRunner` on a recording host with fakes; WASM commands are not executed. Every template ships passing scenarios. 22 CLI tests.
- Verified: `:extension-schema:test :extensions:test :ext-wasm:test :app:testDebugUnitTest :app:assembleDebug`, and `tools/easyide-ext` `test`.
## 2026-09-24 18:05
- M7: added the `easyide-guest` Rust crate (`services/shared/guest-rust`, Apache-2.0): an `Extension` trait, `host::call`, framing, and `export_extension!`, which provides every ABI v1 export and only exports them on wasm32. 3 cargo tests.
- Added `services/shared/samples/wasm-word-count`: a Rust L2 sample. Its built `wasm/main.wasm` (71 KB) is committed, `./build.sh` rebuilds it, and it passes `easyide-ext validate --strict` and `test`.
- `:ext-wasm` `WasmSampleTest` runs the sample in the real `WasmHost`: static check, activation, command registration, editor read, message, result, and `E_CAPABILITY` without `fs.project(read)`. `FakePorts` now records `ui.showMessage`.
- Not done: the app does not host WASM yet (`:app` does not depend on `:ext-wasm`), the AssemblyScript bindings, the `wasm-rust`/`wasm-assemblyscript` templates, and a view provider in the sample.
## 2026-09-24 16:30
- Customization UI half of M4 (customization.md sec 6, 7.3, 10, 12, 16). `:extensions`: `ContributionOverrides` (hide minus `NON_HIDEABLE`, `order` per location: default sort, then listed ids first; `withHidden`/`moved` whole-list edits). New keys `workbench.contributions.order` (P, OBJECT) and `keyRows.layouts` (G) in `WorkbenchSettingsSchema`; `MenuModel`, `StatusItems`, `KeyRows` take the order, `KeyRows` takes user layouts (replace by id, user rows first), `UserKeyRows` decodes them with per-row problems.
- UI: Extensions inspector Hide/Show and Move up/down (menus, status bar sides, key rows); `keyRows.active` picker; Language servers and Keyboard shortcuts full-screen dialogs from Settings; JSON editor suggestion chips (`SettingsJsonCompletion`: keys, `[lang]` L-scope keys, enum/boolean values, keybindings `command` ids). `ServerConfigMerge` reports `ServerRejection` reasons and `fieldProblems`; `KeymapResolver.Result.userEntries`; `KeybindingList` (provenance, `when`-overlap conflicts, add/remove edits keeping comments); JSON editor warns on bad/non-hideable refs, skipped key rows, incomplete custom servers and wrong-typed server fields.
- Touched outside settings/extensions screens: `workspace/ext/WorkspaceExtensionUi.kt` (additive: passes order, user rows and the `NON_HIDEABLE`-filtered hidden set). Moved the `Nls.candidates` assertion from `:extensions` ManifestParserTest (it no longer compiled against the now-internal `Nls`) into `:extension-schema` `NlsTest`.
- Deferred: `workbench.stages.placement` (no contributed stage is rendered; `WorkspaceBridge.revealStage` knows built-ins only), prefix/reserved chord conflicts, rebind/reset. The profile file's `keyRows` field still only round-trips; layouts live in the profile's settings as `keyRows.layouts`.
- Verified: `:extension-schema:test` (10), `:extensions:test` (132), `:app:testDebugUnitTest` (314), `:app:assembleDebug`. No device run.
## 2026-09-24 18:55
- M7: added `@easyide/guest-as` (`services/shared/guest-as`, Apache-2.0): ABI v1 framing, `call`, an `Extension` class with `activate`/`handle`, and a minimal `Json` (quote, str, field).
- `init` templates added: `wasm-rust`, `wasm-assemblyscript` and `lsp-pack`, which completes the seven templates sdk-reference names. The WASM templates vendor their bindings, copied from services/shared at CLI build time, and golden tests build them with cargo and npm when those are present.
- ABI v1 addition: an optional `_initialize` export. Every AssemblyScript runtime needs initialization, and a start function is refused, so `ModuleValidator` accepts `_initialize` with type `() -> ()` and `WasmInstance.initialize()` runs it once before `ext_abi_version` under the activation fuel and time limits. Tests: `reactor_init.wat`, `bad_initialize.wat`, and `WasmSampleTest` running the new AssemblyScript sample (`services/shared/samples/wasm-word-count-as`, built module committed).
- Verified: `:extension-schema:test :ext-wasm:test` (71) and the CLI `test` (22).
## 2026-09-24 16:30
- M4 language features and customization (branch `worktree-agent-a90fcfc6c69e59dcf`, based on `e6772f9`, not on the newer `claude/ecstatic-mccarthy-ui780u` head that has the contributed-theme commits):
  - `ServerGates`: safe mode disables `lsp.servers` entries defined without an extension; `lsp.enabled` resolves per language, so a server keeps only its enabled languages and is disabled when none remain.
  - Semantic tokens: `TOKEN_OVERLAY` plus the `SemanticRules` token types and modifiers are advertised. `SemanticTokensController` requests full/delta for the visible document (range first above 2,000 lines, and again on refresh). `SemanticOverlay` holds per-line spans and shifts them onto newer text. `SemanticPaint` adds them after the TextMate spans of each line. `SemanticStyler` handles theme and customization selectors. Gated by `editor.semanticHighlighting.enabled`, which accepts true/false/`configuredByTheme`.
  - Code lens is advertised through the gutter layer (`REQUIRED_LAYER` now accepts any one of several layers). `CodeLensController` resolves lenses lazily for visible lines. A gutter tap lists the line's lenses in an `EditorPopup`. `editor.action.showReferences` opens References; other commands go to `workspace/executeCommand` on the lens's server. `editor.codeLens` gates it. `LspClient.refreshes()` exposes server refresh requests, and inlay hints now re-run on `workspace/inlayHint/refresh`.
  - `ThemeCustomizer` merges `workbench.colorCustomizations`, `editor.tokenColorCustomizations` and `editor.semanticTokenColorCustomizations`, including `[Theme]` blocks, in the 8.3 order. The highlighter paints role `fontStyle`. `ThemeTokens` carries the semantic rules and the theme's `semanticHighlighting` flag.
  - `workbench.iconTheme`: `ActiveIconTheme` and `IconThemeFile` provide PNG icons for the explorer and a Settings picker row. SVG and font icons are skipped and logged.
- Edits to files owned by other sessions, all small and additive: `EditorPane` (a `semanticTokens` parameter passed to the highlight pass), `WorkspaceScreen` (collects the overlays and the theme flag), `FileTreePane` (themed image through `LocalFileIcons`, with the built-in icons as fallback), `SettingsScreen` (one branch for the picker row), `MainActivity` and `Theme.kt` (customizations and the icon theme locals).
- Not done: inlay hint resolve and tap-to-accept, because they need a hit target on the ghost text in `EditorPane`. Two follow-ups for after the merge with the theme commits: pass `workbench.colorTheme` as `EasyIdeTheme(themeLabel)`, and have the theme file reader fill `VsCodeColorTheme.semanticHighlighting` and `fontStyle`.
- Verified: `:lsp:test` (119 tests) and `:app:testDebugUnitTest` (339 tests) pass, and `:app:assembleDebug` builds. `:extensions:test` does not compile. This was already the case before this work: `ManifestParserTest` uses the `internal` `Nls` class, which moved to `:extension-schema`. No device run.
## 2026-09-24 19:30
- M6 app side, installing from a signed static registry (registry-and-install.md sec 3-8, 10, 12), in `app/extensions/registry/`:
  - Settings `extensions.registries` (default: none, since there is no public index yet) and `extensions.autoCheckUpdates` (weekly; checked at app start and when Extensions opens; only notifies).
  - `RegistryClient`: https-only `HttpFetcher` port with ETag/304 and `RegistryPolicy` limits; files are staged, verified with anti-rollback, then swapped in with a rename. `meta.json` holds fetchedAt and ETags. Publisher files are fetched lazily and verified; offline, the last verified copy stays in use and its age is shown.
  - `TrustStore`: pins live in `state.json` and are written only on commit. A rotation re-pins; a pin can be forgotten.
  - `RegistryInstaller`: steps are engines, the content-addressed cache with a `.partial` file, `checkBytes`, `trustEntry`, local staging, a manifest-vs-entry check, commit as `REGISTRY` recording the signer, then the pin. A cache hit makes no network call.
  - Revocation: installed versions are disabled after a refresh and never uninstalled. `LocalInstaller.isRevoked` refuses revoked versions at staging and at rollback.
- UI: the Extensions screen has a Browse tab with its state in `BrowseState`, a detail sheet and a refresh action. The install sheet shows "registry <id>, signed by <keyId>", and installed rows show update and revoked badges.
- Tink `Ed25519Verify` (1.23.0) is in `:app`. Recorded as an amendment to 0016 and a NOTICE row.
- Open VSX was skipped.
- Verified: `:extension-schema:test` (110), `:extensions:test` (41), `:app:testDebugUnitTest` (357, 14 of them new registry tests), `:app:assembleDebug`. No device run.
## 2026-09-24 09:53
- M7 app side (branch `m7-wasm-app`): `:app` now depends on `:ext-wasm`. `app/extensions/wasm/WasmRuntime` builds the process's `WasmHost` (cache `<files>/extensions/wasm-cache`) and plugs into `ExtensionsRuntime` as its `LogicHost` and a WASM `Activator`. Activation happens on the manifest's activation events, and the instance is dropped when the extension is disabled, uninstalled or its environment is left. `onTrimMemory` from RUNNING_LOW and the `extensions.wasm.enabled` switch are honoured. A crash loop is logged and persisted through `ExtensionInventory.setCrashDisabled`.
- Every host port is bridged to app services:
  - editor and lsp through `WorkspaceBridge`/`LspRequestGateway`, `E_UNAVAILABLE` with no workspace;
  - `fs.*` mapped into the project dir or rootfs, refusing symlink escapes;
  - prompts through the L1 prompt host, and runtime status items merged into the status bar;
  - `config.*` with the L1 unwritable list;
  - `sandbox.exec` through `startCaptured`/command terminals, with streamed output events;
  - `net.fetch` over `HttpURLConnection`, with public-address checks, declared-host redirects and a body cap;
  - storage as JSON files per extension and scope;
  - clipboard behind `ClipboardAccess`;
  - secrets always return null.
- Module sha256 is recorded at first load (`ModuleShas`). WASM completion and hover providers merge after LSP through `LspRuntime.extensionProviders`. `CommandOutcome.Failed` now carries an error code, so a denied host call is logged as `E_CAPABILITY`.
- Edits outside my area, all small and additive:
  - `WorkspaceBridge`: `projectDirectory`, `activeDocument`, `select`, with defaults.
  - `AppHostPort.workspaceBridge`.
  - `WorkspaceExtensionHost`: those overrides, and the new `WasmEventFeed` for `workspace.*` and `editor.didChangeSelection`.
  - `WorkspaceExtensionUi`: dynamic status items.
  - `CompletionController`/`InfoController`: an optional providers parameter.
  - `WorkspaceLspController`, `LspRuntime`, `ExtensionsContainer`, `AppContainer`, `EasyIdeApplication`: wiring.
  - `ActionRunner`/`HostPort` in the shared core.
  - No change to WorkspaceScreen, WorkspaceViewModel or EditorPane.
- Not done: contributed views are not rendered, so view data is only stored. Also missing: `editor.decorate`, `lsp.notify`, `fs.watch` for changes made inside the environment, a secrets UI, and a device run.
- Verified: `:extension-schema:test` (110), `:ext-wasm:test` (71), `:extensions:test` (41), `:app:testDebugUnitTest` (414 tests, 29 of them new WASM tests; `WasmRuntimeTest` runs the Rust sample from disk through the runtime to "4 words"), and `:app:assembleDebug`.
## 2026-09-24 21:30
- M5 app side (branch `m5-app` on `b13b043`): the in-app dev loop.
  - `extensions.developerMode` (G, default off) in the new `AuthoringSettingsSchema`, shown through the generic Settings row. It was already listed in `SettingsPolicy.APP_LEVEL`.
  - `app/extensions/dev/`: `DevReload` holds the pure rules: inbox and request lookup, folder mapping that refuses absolute paths, `..` and escapes through links, `<version>-dev.<n>` versions, and prompt vs silent. `DevInstaller` stages through `LocalInstaller` (new `adjust` hook) and commits as `Source.DEV`. It prompts on a first install, when replacing a non-developer copy, and when capabilities change; otherwise it reloads silently. Each outcome is a toast plus an Extension Log line, and a waiting sheet opens on the Extensions screen. `DevReloadReceiver` handles the adb push and broadcast. `DevLoop` wires it all and watches the open project's `.easyide/dev/` with `ProjectFileWatcher`.
  - Every reload gets its own version because the runtime treats version dirs as immutable and reloads only when `(id, version)` changes. A same-version replace in place would not show.
  - "Create extension" (Extensions > +): five declarative templates are copied into the APK as assets by the `copyAuthoringTemplates` task in `:app` (one copy in git). Files go to `<project>/<name>/` and are mirrored to a linked SAF folder when the project has one. "Install from this folder" follows, as a developer install when developer mode is on. `ExtensionScaffold` is pure. `FileFolder` drops the author-side files that `package` drops.
  - Shared `dev.easyide.extensions.authoring`: `ExtensionTemplates` (render and substitution) and `IgnoreRules`/`PackageIgnores` moved out of the CLI. `InitCommand` and `PackageSource` use them; `IgnoreRules` is a typealias in the CLI.
- Hooks in files owned elsewhere, all small and additive: `SettingsSchema.all` (+`AuthoringSettingsSchema.all`), `ExtensionsContainer` (`val dev`, `dev.start()` after staging is cleared, `dev.setScope(scope)` in `setRuntimeScope`), `ViewModelFactory` (`projectManager`, `projectRoot`), the `AndroidManifest` receiver, and the `app/build.gradle.kts` asset task. No Workspace*/EditorPane/Home* edits: the project comes from `RuntimeScope`, which `WorkspaceExtensionHost` already sets.
- Needs a device check: that only adb can send the `DUMP`-protected broadcast; that the app can read and delete an adb-pushed file in `Android/data/<id>/files/dev-inbox` on API 30+; that a save shows up on the device in under 5 s. On API < 30 the inbox is writable by apps with storage permission; they still cannot send the broadcast, and a capability change still prompts.
- Docs: author-guide quick start (CLI commands, Create extension, developer mode, dev reload); cli.md status and deviations; registry-and-install 8.4; tracker M5 rows.
- Verified: `:extension-schema:test` (118), `:extensions:test` (41), `:app:testDebugUnitTest` (419, 18 new), `:app:assembleDebug` (the APK has `assets/authoring-templates/`, without the WASM templates), CLI `./gradlew test` (25). No device run.

## 2026-09-24 17:32

- Added the UI redesign design set `docs/ui-redesign/` (arch, shell-model, extension-ui, layout-spec, screens, identity, ux-rules, properties, kit, tracker) and ADRs 0025 (shell model) and 0026 (identity, kit, appearance properties), both Proposed.
- Why: on-device testing and the owner's review found the workspace layout unusable on a phone and Home, Settings and Extensions built from stock Material widgets with no shared rules. The direction (owner, 2026-09-24): a VS Code-style shell where pages open as documents in a main stage, side panels, bottom navigation on phones and a side panel on tablets, all configurable by extensions (Docker, agent, chat examples).
- Research inputs: four read-only investigations (reference apps, layout models, identity and properties, code feasibility). Verified by inspection of the bundled fonts that Geist Mono has no block-cursor glyph (U+25AE) or check mark and neither font has a `zero` feature; the block motif is therefore drawn.
- ux-overhaul Pillar 4 (layout) is marked superseded in part; ADR 0019 notes the amendment.
## 2026-09-24 15:37

- Workspace sessions are now owned by an app-scoped `WorkspaceRegistry` keyed by project id ([0023](../decision/0023-workspace-session-lifetime.md)): leaving a project parks it (shells and buffers stay alive, no prompt), re-entering re-attaches, the new rail "Close project" button ends it behind the existing unsaved-changes dialog. Park limit `workspace.maxParkedProjects` (default 2), LRU eviction with a backup under `onTrimMemory`. New `session/` package (snapshot codec, atomic `SessionStore`, park policy, registry, external-change and path-remap logic) and `workspace/session/` (persistence, restore, external-change checks, scroll/layout holders, conflict dialog). `WorkspaceViewModel`, `WorkspaceScreen`, `EditorPane`, `StageState`, `AppNavHost` edited additively; tab creation extracted to `EditorTabs.kt`.
- Hot-exit and restore: versioned per-project snapshot plus dirty-buffer backups, written on a 2 s timer, on park/evict and in `MainActivity.onStop`; restored after process death or relaunch with conflict handling and a "Restored N unsaved files" notice. Settings `workspace.restoreOpenTabs`, `workspace.openLastProjectOnLaunch` (skipped while a crash report is pending). External-change reload/conflict dialog for open buffers and rename-follow for files and folders (S8).
- `SandboxForegroundService` now runs while a workspace is live (`SandboxKeepAlive`), shows a count and "Stop all", is not sticky and handles the Android 15 timeout. tmux deliberately not built (reasons in 0023).
- Diagnostics: `diagnostics/` (rolling file log, `LogSink`, crash handler and reports, extension-log and LSP-status bridges, collector, symlink-safe cleanup, checksum probe), Settings > Diagnostics screen, crash recovery dialog on Home.
- Why: S1 follow-up (ADR-D), S6, S8 and the Reliability/Diagnostics pillar of the UX overhaul.
- Verified: `:app:testDebugUnitTest :sandbox-runtime:testDebugUnitTest :app:assembleDebug :app:assembleRelease`. Nothing run on a device.

## 2026-09-24 16:35

- Editor workflows on engine-neutral models ([0022](../decision/0022-editing-workflows-history-find-quick-open.md)):
  - Undo/redo: `EditHistory` (patch steps, coalescing by run/pause/word, selection restored, size-capped) per open document in `EditHistories`; typed edits arrive through `EditorSession.onUserEdit`, every other change (LSP, extensions, replace) is caught at `WorkspaceViewModel.onContentChanged`/`replaceContent`. Commands `edit.undo`/`edit.redo` (Ctrl+Z, Ctrl+Shift+Z, Ctrl+Y).
  - Find/replace/go to line: `TextSearch` (literal/regex, case, whole word, 500 ms deadline guard), `FindController` painting `SearchMatches`, `FindBar` (48 dp targets), Ctrl+F/H, F3/Shift+F3, Ctrl+G; replace one/all is one undo step.
  - Quick open (Ctrl+P): `FileIndexer` + `GitignoreRules`/`IgnoreIndex`, `PathMatcher`, per-project recents in `UiPreferences`; `PickerOverlay` now shared by the palette, quick open and go to line.
  - Polish: welcome view from the live `Keymap`, `FileIcon` (kind -> glyph + token tint) in the explorer, explorer filter settings `explorer.hideHiddenFiles` / `explorer.hideGitIgnored`, pinch-to-zoom for editor (`editor.fontSize`) and terminal (`terminal.fontSize`) via `SettingsEditor`.
- Structure: `EditorPane` highlight code moved to `EditorHighlighting.kt`, reveal helpers to `EditorReveal.kt`; new commands are in `CommandIds.EDITING`/`KNOWN`, not `ALL` (the extension-side lists are held equal to `ALL` by tests).
- Verified: see the implementation report. No device run.

## 2026-09-24 17:55

- Merged the four in-flight UI branches into `ui-improvements` (snapshots first, since their work was uncommitted): `ui/reliab` (session registry, restore, hot-exit, diagnostics), `ui/editor` (editing workflows), `ui/git` (identity, credentials, remote ops, branch UI, diff view) and, from `ui/layout`, only the plain-Kotlin logic (`PaneArrangement`, `PaneSizes`, `SplitterMath`, `StageVisibility`, `CompactPane`, `TabHistory`, `TabOrder`, `TreePaths`, `StatusFacts`, `FoldPosture`, `WindowSize.fold`) with its tests; its screen edits are dropped because the shell redesign ([0025](../decision/0025-ui-shell-model.md)) replaces that layout code.
- Conflict resolutions kept both sides: settings schema lists, factory wiring, editor scroll/semantic-token/history parameters (semantic paint ported into `EditorHighlighting.kt`), theme file icons over the built-in glyphs in the explorer.
- Structure: file actions (create/rename/delete/copy/paste) moved out of `WorkspaceViewModel` into `WorkspaceFileActions` (631 -> 571 lines, cap 600).
- Tests fixed: `TextSearchTest` used `(a|aa)+b`, which JDK 21 no longer backtracks catastrophically (now `(.*a){20}b`); the git draft test used `advanceUntilIdle`, which skips background jobs; `WorkspaceGitControllerTest` had never compiled (missing imports, internal JGit handle) and now opens the tree with JGit directly (`testImplementation(libs.jgit)`).
- ADR 0025 and 0026 accepted by the owner; redesign tracker updated.
- Why: the redesign (R1) must not fight open edits in Settings, workspace and Home.
- Verified: `:app:testDebugUnitTest :sandbox-runtime:testDebugUnitTest :extensions:test :lsp:test :app:assembleDebug`. Not yet run on a device.

## 2026-09-24 20:58

- Fixed four bugs found running the Python LSP path on a real tablet (Xiaomi Pad, Android 16): (1) `LanguageConfigs.languageIdFor` only read the bundled grammar index, which no longer has Python, Go, Rust etc. (they live in packs), so no LSP document was ever opened and no server status or install notice appeared; it now asks the highlighter, which checks pack languages first. (2) The LSP path mapper compared `/data/user/0/...` (SandboxPaths) with `/data/data/...` (project files) lexically, so every host file mapped to no guest URI; the locator, rootfs and the workspace controller now use canonical roots. (3) `TerminalSession.write` dropped bytes written before the pty process started, so the Install button opened an empty shell; writes before start now queue (only a finished session drops them). (4) The Python pack installed ruff with `pipx` into `~/.local/bin`, off the sandbox PATH, so the probe never found it; it now installs to `/usr/local/bin`.
- Also: Home's language dot counts a root file double (a starter `main.py` beside `src/app.js` was reported as JavaScript).
- Why: M2's exit test (Python server starts and shows diagnostics) had never passed on a device.
- Verified: on the tablet, Install ran the recipe, pyright started, completion and an undefined-name diagnostic appeared; `:app:testDebugUnitTest --tests PythonPackTest --tests ProjectLanguageTest`. ruff's new install path is not re-run on the device yet.

## 2026-09-24 21:23

- Added the kit layout primitives and motif drawables in `ui/kit`: `KitScaffold`, `KitSection`, `KitGroup`, `KitRow`, `KitEmptyState`, `KitProgress` (cell-fill bar; indeterminate cursor sweep of 8 cells at 90ms after a 300ms delay), `CursorBlock` (solid, hollow, blinking; the only infinite transition), `CropCorners`, `PromptGlyph`, `CellFillBar`, plus `KitInteraction` (pressed/hover/focus-ring/selected-marker modifiers), `KitAction`, `MotifLevel` and `res/values/strings_kit.xml`. Unit tests for the cell-fill math, sweep timing, blink phase, motif gating and row minimum height.
- Why: R0 kit foundation of the UI redesign (docs/ui-redesign/kit.md sections 3 and 3.1); screens are migrated onto these in R1. `KitGroup` separators are each child's top rule, with the column shifted up one hairline so the first is clipped.
- Verified: `:app:testDebugUnitTest --tests '*ui.kit*'`, `:app:compileDebugKotlin`, `:app:assembleRelease`. Not seen on a device or in a screenshot; the kit has no gallery yet.

## 2026-09-24 22:10

- Added the kit input primitives in `ui/kit` (one file each, under 150 lines): `KitField`, `KitButton` (with `KitAction`, cursor-sweep loading), `KitIconButton`, `KitTag`, `KitBanner`, `KitDialog` (bottom sheet on compact width, actions mirrored for left-handed), `KitTabs` (underline and segmented), `KitToggle` (drawn switch, check, radio), `KitMenu` (`KitMenuItem` model, window-clamped placement), `KitChoice`, `KitStepper`, plus internal `KitSizes` and `KitInteraction` (hover/press wash, focus ring, chrome mono) and `strings_kit_input.xml` (`kitin_` strings).
- They read colours, spacing, radius and type only through `Kit`/`Tone`, use the 44dp touch floor, roles and state semantics, and play haptics through `Haptics`; no Material widget is used. `ChromeControls.kt` is untouched.
- Why: R0 needs the kit before screens migrate off stock widgets ([kit.md](../ui-redesign/kit.md) section 3).
- Verified: `:app:compileDebugKotlin`, `:app:testDebugUnitTest --tests '*ui.kit*'` (41 tests on the paint, edge, placement, menu, tab and stepper rules), `:app:assembleRelease`. Not seen on a device: layout, dialog window gravity and popup placement are unexercised.

## 2026-09-24 21:40

- Added the pure shell engine under `services/mobile/app/src/main/java/dev/easyide/app/ui/shell/` (R1 of the UI redesign, no Compose and no screen wiring yet):
  - `DocumentUri` (canonical, malformed values rejected), `DocumentRegistry` (type by URI pattern, openers, user associations, "unavailable" placeholder), `EditorGroup` and `EditorStage` (preview, pin, MRU, per-group history, one to four groups, open beside), `ShellState` and `ShellReducer`, `BackNavigation` (shell-model.md section 11), `LayoutPresets` per arrangement, `NavRegistry` and `ContainerRegistry` (extension items never outrank core, user order/hide/pin, five-plus-More split), and `ShellSnapshot` (versioned JSON, tolerant restore).
  - Reuses `PaneArrangement`, `StageVisibility`/`StageRule`, `PaneSizes`, `CloseScope` and `TabOrder` from the salvaged layout logic, and `WindowSize`.
- Why: the redesign's structural core has to be testable on the JVM before any screen is rebuilt on it. Where the spec contradicts itself the simpler reading was taken; the choices are listed in the agent report.
- Tracker R1 rows now say "in-progress: engine done, UI not built".

## 2026-09-24 22:10

- R0 tooling for the UI redesign: (1) `ImportRatchetTest` fails on raw Material widget imports outside `ui/kit` against `ui-ratchet-allowlist.txt` (today: 55 files, 202 imports; the count may only shrink, the regeneration one-liner is in the test's KDoc); (2) `tools/ui-lint.sh` runs seven greppable rules of ux-rules.md section 12 in 0.3 s against `tools/ui-lint-baseline.txt` (12 entries); (3) Roborazzi 1.75.0 + Robolectric 4.17 spike passed, so both are now test dependencies (version catalog) with one gallery golden in `app/src/test/screenshots`.
- Spike findings: needs `isIncludeAndroidResources`, the `--add-exports=java.base/jdk.internal.access=ALL-UNNAMED` test JVM arg, and `@Config(sdk = [35])` (Espresso fails on Robolectric's SDK 37). Licenses checked at source: Roborazzi Apache-2.0 (POM, repo), Robolectric MIT (LICENSE, POM). Details in `docs/ui-redesign/kit.md` section 7.
- Why: the redesign needs a ratchet against raw Material widgets, cheap lint for token rules, and a JVM screenshot path before the kit gallery is built.
- Verified: `:app:testDebugUnitTest`, `:app:verifyRoborazziDebug` (and it fails on a changed pixel), `:app:assembleRelease`; not run on a device.

## 2026-09-24 23:05

- Added the kit gallery: `ui/kit/gallery` (six files, each under 250 lines) shows every kit primitive in its states (tones, enabled/disabled/selected/loading/error, mono, long text, all three dialog kinds, menu) under live controls for density, corners, font scale (1, 1.3, 2 as the system size), accent, theme mode (system, light, dark, amoled), motif and reduce motion. The controls build a local `Appearance` and pass it to `EasyIdeTheme`, the path the app uses; nothing is saved.
- Reachability: the gallery sources live in `app/src/devtools` and are added to the `debug` and `canary` source sets only; `src/release` holds a no-op twin of `ui/devtools/DevTools.kt`. The entry is Settings > Diagnostics > Developer tools > Kit gallery (`DiagnosticsScreen` got an `extraSections` slot; `AppNavHost` registers the route). Its strings are in `src/devtools/res/values/strings_kit_gallery.xml`, so release has none of them. No command-palette command was added (the palette is workspace-only and `CommandIds.ALL` is held equal to the shared pack list).
- Tests: Robolectric 4.17 and `ui-test-junit4` (same three settings as the screenshot spike: `isIncludeAndroidResources`, the `jdk.internal.access` export, SDK 35) run 13 semantics tests (role, name, enabled/selected/toggle state, 44dp hit box for every interactive primitive) and 3 gallery smoke tests (every section composes, density and font scale controls change layout) in `src/testDebug`; `GalleryConfigTest` covers the control-to-`Appearance` mapping. Release unit tests do not exist in this build (AGP creates only `testDebugUnitTest`), so release absence is checked in the R8 output instead.
- Added `tools/ui-device-check.sh`: sets font_scale 1.0, 1.3, 2.0, dumps the uiautomator hierarchy, reports clickable nodes under 44dp or without text or content-desc as `file:line: message`, restores the font scale on exit.
- Why: the owner needs to see and test the primitives on a real device before screens migrate onto them (kit.md 7).
- Verified: `:app:compileDebugKotlin`, `:app:testDebugUnitTest --tests '*ui.kit*'` (75 tests), `:app:assembleRelease`, `:app:assembleCanary`. Not run on a device; `ui-device-check.sh` was only exercised against a synthetic dump.

## 2026-09-24 23:05

- Rebuilt Home on the kit (`ui/screens/home`, R1 of the UI redesign), scaffold-free so the shell can host it: `HomePanel` (header with the "+" menu, Resume hero with the one filled action, Running, Environment, Projects with search, recent/name sort and a long-press or right-click menu), `HomeNowPage` (the same live sections for the stage of a wide window), `ProjectPage(projectId)` (details, environment tag, git summary, recent files, this project's sessions, Open / Open with terminal, "..." menu) and `HomeDialogHost` (rename, duplicate, delete with the name typed, change environment, clone, import, an Install Linux prompt when cloning or importing with no environment, and a stop-session confirmation), all from `KitDialog`, `KitField`, `KitButton`, `KitChoice`. No Material widget remains in the package; `ProjectCard`, `ProjectDetailPane`, `HomeProjectList` and `HomeEmptyStates` are deleted. `HomeScreen` stays only as a host of the new pieces for `AppNavHost`.
- Running is assembled by the pure `RunningAssembly` from a small `RunningSource` interface, adapted (`LiveRunningSource`) onto `WorkspaceRegistry.liveIds` and `LanguageServerManager.statuses` (RSS per server) plus provisioning environments; nothing is invented where a source is missing (no per-terminal read API, no install percentage). `HomeViewModel` takes the source, exposes `resume`, per-project recent-file scans and `onStop`; `AppViewModelFactory` passes the live source. `HomeCallbacks` gained three defaulted members (`onOpenProjectPage`, `onOpenDocument`, `onStop`) so the current `AppNavHost` still compiles.
- Copy moved to the identity voice (short ages "3m", "Couldn't clone: ...", no "Something went wrong"); unused Home strings deleted.
- Why: docs/ui-redesign/screens.md sections 2 and 3; Home is the first destination the shell hosts.
- Verified: `:app:testDebugUnitTest --tests '*screens.home*'` (new: running assembly, resume target, branch label, delete confirmation, process size), `:app:compileDebugKotlin`, `:app:assembleRelease`. Not seen on a device: layout, the long-press trigger's interplay with the row tap, dialog focus and the folder picker are unexercised. The baseline-profile generator taps the first child of Home's scrollable, which is now the Resume section, not a project row.

## 2026-09-24 23:05

- Rebuilt Extensions on the kit under `ui/screens/extensions` as scaffold-free content for the shell to host: `ExtensionsPanel(selectedId, onSelect, viewModel)` (Installed/Browse `KitTabs`, `KitField` search, `KitRow` per pack with mono id, tabular version, toggle, source and state `KitTag`s, safe-mode/needs-approval/revoked `KitBanner`s, the extension log with copy, install `KitMenu`: registry, file, folder, create), `ExtensionPage(id, viewModel, onOpenTarget, onClosed)` (header, Details, Contributions grouped by kind from the runtime registry with hide/show/move, Capabilities declared vs granted with revoke and grant, Log filtered to the pack, Versions with update and roll back, an uninstall confirmation, and a Settings link as `easyide://settings/extensions#<id>`), and `ExtensionsDialogs(viewModel)` (install approval with the capability list, Browse detail, rollback, create extension; all `KitDialog`, `KitField`, `KitChoice`, `KitBanner`, `KitProgress`). Pure row, filter, grouping, tag, capability-diff, contribution-group and log-line logic is unit-tested.
- `ExtensionsScreen` stays as a thin `KitScaffold` around the panel and page until the shell swaps in; `BrowseSection.kt` is deleted, 12 strings it and the old screen used are removed, new copy is in `strings_extensions_ui.xml`.
- Added `LocalInstaller.setApproved` (with `ExtensionsViewModel.setGranted`) so a single capability can be revoked or granted again: revoking leaves the pack in "needs approval" (enablement rule 5) until granted. No such API existed.
- Why: R1 "Extensions on the shell" ([screens.md](../ui-redesign/screens.md) section 4). Registry installs record no signer in the inventory, so the page states the trust of a registry install in words and does not show the key id (the Browse detail still does).
- Verified: `:app:testDebugUnitTest --tests '*screens.extensions*' --tests '*extensions*'`, `:app:compileDebugKotlin`, `:app:assembleRelease`. Not seen on a device or in a screenshot.

## 2026-09-24 23:30

- Hosted the app-scope shell in Compose (R1, phase SHELLUI): `ui/shell/nav` (`NavSurface` bottom bar or rail with More sheet on `KitDialog`, badges, `NavRules`, `NavItemSource` stub, Material icon fallback) and `ui/shell/host` (`ShellHost`, `AdaptiveScaffold`, `ShellBody`, `PanelHost` with a draggable persisted width via `PaneSplitter`/`PanelSizing`, `StageHost` with `DocumentStrip`, `DocumentRenderer`/`PanelRenderer` contracts and registries with the engine's "unavailable" placeholder, `ShellViewModel` with debounced snapshot save and restore, `AppDocuments`). `AppNavHost` now launches into the shell for Home, Settings and Extensions (the Settings and Extensions routes are gone; the workspace's links select the shell destination and pop to it). `shell.*` properties live in a new `ShellSettingsSchema`, registered in `AppContainer` rather than `SettingsSchema`. Added `ShellAction.ResizePane` and an `easyide://project/<id>` page shape.
- Why: the redesign needs one host for every app-scope destination before Home, Settings and Extensions are rebuilt on it. The pre-shell screens are hosted through temporary adapters (see the tracker's R1 swap list) so the app works at every commit.
- Verified: `:app:testDebugUnitTest` and `:app:assembleRelease` (R8). Not seen on a device: layouts, insets, IME hiding of the bar, splitter drag and Back behaviour are unexercised.

## 2026-09-24 23:30

- Rebuilt Settings on the kit as scaffold-free content the shell hosts (`ui/screens/settings`, R1): `SettingsPanel` (search with `@modified`, `@ext:`, `@lang:` filters, User / Environment / Project control with an inline target picker, safe-mode banner, category list or ranked results), `SettingsPage(category)` for Appearance, Editor, Terminal, Files, Git, Sandbox, Keyboard, Language servers, Extensions, Layout, Diagnostics, Advanced and a search-results page. Rows are generated from the schema: modified dot, layer badge (`KitTag`), per-row reset, "overridden by" note, protected-key and per-language handling, inline choices, steppers, text fields. Page assignment is `SettingsCategory.of` (contributed settings go to Extensions).
- New pages: Appearance (live preview built from kit primitives, theme cards, accent swatches plus hex field showing the contrast-guarded colour and a hue warning near signal colours, every `appearance.*` row, "Reset appearance") and Layout (`shell.layout.preset`, navigation position and labels, navigation items with move up/down, hide and pin, container placement and hide). The `shell.*` keys are declared in the new `ui/props/ShellSettingsSchema` (with `NavPrefs` / `ContainerPrefs` readers); `SettingCategory.LAYOUT` was added; `SettingsSchema.all` was not touched.
- Dialogs and pickers moved to `KitDialog` / `KitField` / `KitButton`: profiles, import preview, reset, project trust, git credential forget, add shortcut, language server form; the JSON editor is a `KitScaffold` overlay (a plain text field keeps the caret for completion). Keyboard shortcuts and Language servers are pages, not dialogs. `SettingsScreen` remains as a thin adapter (panel + page, list then push on a phone) for the current navigation; deleted `SettingsLayerBar`, `KeybindingsDialog`, `LanguageServersDialog` and ten unused strings.
- Why: docs/ui-redesign/screens.md 5 and properties.md 7; settings are documents with a category list panel, and the redesign's properties need a home in the UI.
- Verified: `:app:testDebugUnitTest --tests '*settings*' --tests '*props*'` (new pure-logic tests: categories, row state, layer control, layout ordering, accent feedback, search, shell schema), `:app:compileDebugKotlin`, `:app:assembleRelease`. Not seen on a device: layout, dialog placement, scroll-to-result and the preview are unexercised.

## 2026-09-24 23:45

- Integrated the four R1 branches (R1, phase INTEGRATE). One `ShellSettingsSchema` (`ui/props`, with `navSettings`, `navPrefs`, `containerPrefs`) registered once in `SettingsSchema.all`; the `data/settings` copy, the `AppContainer` concatenation and the duplicate `shell.*` and `nav_*` strings are gone, and a test fails on any duplicate key.
- Swapped the adapters for real bindings (`ui/shell/host`: `AppRenderers`, `HomeBinding`, `SettingsBinding`, `ExtensionsBinding`): Home, Settings and Extensions panels in the primary panel, project / settings / extension pages in the stage, `HomeNowPage` and the Appearance page as the stage default of a wide window, `HomeDialogHost` and `ExtensionsDialogs` mounted once. Tabs and the compact title bar show the subject's name through the new composable `DocumentRenderer.Title`. Added a shell toast (`ToastQueue`, `ToastHost` on `KitBanner`, `ShellActions.notify`) for `SettingsHost.onNotify`, `LayoutCatalog.of(registries)` with `packOf` on both registries, and `SettingsLink` so `easyide://settings/extensions#<id>` marks that extension's first row. Deleted `LegacyAdapters.kt`, `PanelBinding.spansWindow`, `HomeScreen`, `SettingsScreen`, `ExtensionsScreen`, `HomeUiState.detailOpen` / `onCloseDetail` and the list-pane metrics; Settings got its own title row so each panel has exactly one header.
- Ratchet allowlist regenerated: 55 files / 202 imports down to 29 files / 87 imports. `tools/ui-lint.sh` clean, baseline unchanged. The baseline-profile generator now taps `kit:project-row` then `kit:project-open` (`ShellHost` exposes test tags as resource ids).
- Why: the shell was hosting the pre-shell screens through window-spanning adapters; the rebuilt kit screens could not be reached until the panels and pages were bound.
- Verified: `:app:testDebugUnitTest` (new tests: renderer bindings against the registries, toast queue, layout catalog from registries, settings link, merged shell schema), `:app:assembleRelease` (R8), `:app:assembleCanary`. Not seen on a device: the whole shell flow, toast placement, scroll-to-row from the link and from search results, and the regenerated baseline profile.

## 2026-09-25 09:30

- Rebuilt Onboarding, New project, Install Linux and Diagnostics on the kit (R2, agent FLOWS). Onboarding: `KitScaffold` + `KitStepper` in the bar, the step in a crop-cornered card with the title typed once (60 ms per character, whole under reduce motion), actions pinned in the thumb zone, real install progress on `KitProgress` with the phase and log tail in mono, cancel, retry or resume. On expanded windows and phone landscape (`onboardingLayout`) the steps sit beside a rail (mark, the step list with done / now tags, install progress); portrait phone and small tablet keep one column. New project and Install Linux use `FlowFrame` (full screen on compact, a centred sheet-style panel on wider windows); New project rows use `KitRow(selected)`, presets show a `ready` tag when an installed environment came from them. Diagnostics is `KitScaffold` + `KitSection`/`KitRow` with the `extraSections` LazyListScope hook unchanged; its two dialogs are `KitDialog` (the crash dialog keeps two actions, Share moves into the content when it can also reopen).
- Deleted `ChoiceCard`, `EnvironmentBadge`, `ContextMenu` (unused); added `FlowFrame`, `ProseText`/`MonoText`, `EnvironmentState.tone()`. `EmptyState` stays because the workspace (`ProjectNotFound`, `SourceControlPane`) still uses it. Copy moved to `res/values/strings_flows.xml` in the identity voice; "Get started" is unchanged, so the baseline-profile selector is unchanged.
- Ratchet allowlist regenerated: 29 files / 87 imports down to 20 files / 58 imports (all nine files of this scope left it). `tools/ui-lint.sh` clean, baseline unchanged.
- Why: docs/ui-redesign/screens.md 6, 7, 10 and layout-spec flows; the narrow centred onboarding column left half of a landscape tablet empty.
- Verified: `:app:testDebugUnitTest` (1295 tests, new: layout choice by window size, rail step states, step buttons, preset ready, flow presentation, plus Robolectric bounds checks of the onboarding frame at 900x1440dp, 1440x900dp, 800x360dp and 393x851dp and render checks of every install state), `:app:assembleRelease` (R8), `:app:assembleCanary`. Screenshots reviewed on the JVM only. Not seen on a device: real windows, the system permission screens, keyboard behaviour of the sheet, baseline profile regeneration.


## 2026-09-25 01:00

- Rebuilt the content of the workspace panels on the kit (R3, phase WPANELS), leaving every public signature the workspace structure calls unchanged. Files: `FileTreePane` (rows at least the touch floor tall, block-marker selection, indent guides kept, filter behind a `KitMenu`). Source control: `SourceControlPane`, `ScmHeader` (branch as a `KitRow`, sync as ghost `KitButton`s, overflow `KitMenu`), `ScmCommitBox` (`KitField`, amend toggle row, identity form; the commit button waits while the form is open so there is one primary), change rows on `KitRow` with two trailing actions, `OperationPanel` (failure as `KitBanner` with the token action), `BranchSheet` / `RemoteSheet` / `StashSheet` as `KitDialog` (the start-point chooser is an inline list, not a menu inside a dialog), confirmations and `GitCredentialDialog` on `KitDialog` / `KitField`. LSP: `LspSidePanel` on `KitTabs`, Problems with severity `KitTag` filters, References and Outline on `KitRow`, the status item opens a server list dialog (restart, stop, start, install, retry, log) instead of a menu, the install notice is a `KitBanner`, rename preview, server questions and the symbol picker (now on the palette's `PickerOverlay`, which adds arrow-key selection) moved over, popups keep their layout with kit tokens. Also `FindBar` (find options as `KitTag`, actions as `KitIconButton`), the terminal tab strip (`KitTabs`, new and a rename/close menu, install as `KitButton`) and key row, `WelcomeView` (`KitEmptyState` plus `KitSection` rows for recent files and the live shortcut list; new optional `onNewFile`), Markdown preview chrome, `ExtensionSlots` (`KitMenu`, `KitIconButton`) and `ExtensionPrompts` (`KitDialog`, `KitField`, password masking).
- Kit change: `KitField` takes a `visualTransformation` (token and password fields). New shared pieces: `PanelChrome.kt` (title row, group header, touch-floor row height) and `DialogParts.kt` (keyboards, heading, text). `ChromeControls.kt` (`DenseTextField`, `ChromeButton`) is deleted; `MenuModel`, `StatusItems`, `KeyRows`, `ActiveKeyRow` and their tests are untouched. Deleting a folder now asks for its name (U-CMP-04). The old `</` watermark on the empty editor is gone (R5 draws the new mark). Copy is in `strings_wpanels.xml`; three panel tab labels and two Welcome headers lost their caps or length.
- Why: [screens.md](../ui-redesign/screens.md) 8 and 9 and kit.md 5 (workspace stock leftovers). Import ratchet: 29 files / 87 imports down to 9 / 29 (every workspace file is off the list); `tools/ui-lint.sh` clean, four stale baseline entries dropped.
- Verified: `:app:testDebugUnitTest`, `:app:assembleRelease`, `:app:assembleCanary`. New JVM tests: delete confirmation, server actions and severity tones, contributed menu items. Not seen on a device: sheets and dialogs on compact and wide, the file context menu position (it opens where `WorkspaceScreen` mounts it), tab strip and key row heights at the 44dp floor, focus and Back in the dialogs.

## 2026-09-25 09:00

- Rebuilt the workspace structure on the shell (R3 core, phase WSHELL). `ui/shell/workspace`: `WorkspaceShell` (the app shell's `NavSurface` and `AdaptiveScaffold` over a workspace `ShellState`; the bottom bar steps aside for the keyboard), `WorkspaceBody` (docked sidebar and secondary panel with drag edges, side sheets with a scrim on a phone, the stage over a resizable bottom panel, book and tabletop pages split at the hinge), `BottomPanel` (Terminal and Problems tabs, kept composed while hidden, three detents on a phone that never go below 160dp, 25-70% docked), `StatusStrip` (priority fit by measured width: nothing overlaps), `InputDock` (touch toolbar and key rows above the keyboard, replacing the bottom bar), `LayoutPresetPicker`, `WorkspaceEffects` (file documents follow the view model's tabs, the language panel and the Problems/Outline containers follow each other, source control refreshes on open, a terminal reveal request opens the terminal). Pure and JVM-tested: `WorkspaceLayout`, `BottomSizing`, `StatusPlan`, `DockRules`, `FileTabSync`, `WorkspaceNav`, `WorkspaceShellModel`.
- Files are `file:///workspace/<path>` documents rendered by the existing `EditorPane` (buffers stay in `WorkspaceViewModel`); Settings and Extensions are containers in the workspace rail and their pages open as `easyide://` documents in the stage through the app-scope renderers; Back to projects, Command palette and Close project are navigation items with command targets. `StageHost` now draws 1-4 groups (document switcher on a phone, tab strip with split and close-group actions on a wide window).
- The workspace layout persists in the session snapshot as `ShellSnapshot` JSON (`SessionSnapshot.shell` replaces `LayoutSnapshot`); `WorkspaceScreen` went from 503 to about 410 lines; `WorkspaceChrome`, `WorkspacePanes`, `StageState`, `EditorTabBar` are gone. Search is a file-name search over the project index (there is no text search engine yet).
- Why: on a phone the explorer covered the editor without a scrim, the terminal collapsed to a sliver and the status text overlapped ([shell-model.md](../ui-redesign/shell-model.md) sections 7-12, [layout-spec.md](../ui-redesign/layout-spec.md) section 4).
- Verified: `:app:testDebugUnitTest`, `:app:assembleRelease`, `:app:assembleCanary`, `tools/ui-lint.sh`. Not seen on a device or in a screenshot.

## 2026-09-25 14:00

- Built split view and the git documents (R6, agent DIFF). The diff is a stage document, not a dialog: `git-diff:///<repo path>?base=<end>&head=<end>` (`Comparison`, ends are a revision, `index`, `worktree` or `empty`) drawn by `GitDiffDocument` (unified, or two columns when the document itself is 840dp wide; hunk navigation, stage / unstage / discard hunk where the pair is the index or the working tree, word-level highlighting, the editor's TextMate grammar for colours) and `git-commit://<sha>` by `GitCommitDocument` (message, author, date, parents, files that open their diffs). Both are registered in the workspace registry, restorable and placeholder-safe. `DiffScreen` and `GitDiffState` are deleted; `GitDiffController` keeps the hunk actions and publishes a revision that makes open diffs re-read. `sandbox-runtime` gained `GitService.compare` (any two ends) and `commitDetail`.
- Provider seam: `DiffProvider` / `DiffProviders` (`ui/shell/diff`), with the built-in `NativeGitDiffProvider`; a pack cannot register one until the extension runtime has a `compare` contribution (noted in extension-ui.md section 7).
- Open beside and groups: `OpenBeside` rules per size class (one group on a phone opens in place with a message; two on medium, book, tabletop; four on expanded), a tab menu on long press or right click (`TabMenuPlan`: open to the side, move to the next group, keep, pin, close, close others / to the right / all), the same menu without the stage entries on a long press of the phone's document switcher, palette commands `Open to the Side`, `Split Editor`, `Close Group`, `Move Editor Into Next Group` with default keys in the keymap table (`CommandIds.STAGE`, kept out of the pack-facing `ALL`). Settings and extension pages are splittable now; project pages are not. The file menu has `Open to the Side`; a change row previews on tap, keeps on double tap and has a menu.
- Gaps closed: the file context menu, tab menu, change-row menu and editor context menu open under the press (`KitPressPoint`, `KitMenu(at)`); a right click on a tree row opens the file menu; new file, new folder and rename are typed into the tree (`InlineEdit`), so those three dialogs are gone; `KitRow` and `kitPressable` take a long press and a double tap, `KitField` an `inputModifier`.
- Why: shell-model.md section 4 and 8, extension-ui.md section 7, and the R3 leftovers (menus mounted at the screen corner, no close others on the phone).
- Verified: `:app:testDebugUnitTest` (1453 tests; new: comparison URI round trip and malformed input, diff and hunk model, syntax sides, provider registry, git document registration and snapshot round trip, open-beside rules against the engine per size class, tab menu plan, stage actions, keymap chords, inline edit, change row mapping, Robolectric render of the diff list and header), `:sandbox-runtime:testDebugUnitTest` (new `GitCompareTest` on real repositories), `:app:assembleRelease` (R8), `:app:assembleCanary`, `tools/ui-lint.sh`. Not seen on a device: the diff document in either layout, hunk navigation scrolling, syntax colours in a diff, the menus under a real finger or pointer, the inline row with the keyboard, two groups side by side. Gestures and the caret scrub bar stay blocked on the editor engine (PE1).
