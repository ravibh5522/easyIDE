# Feature: Extension SDK & Language Intelligence - Tracker

Status legend: not-started / in-progress / done / blocked

Design: [arch.md](arch.md). API reference: [sdk-reference.md](sdk-reference.md). Everything below is PROPOSED as of 2026-09-23.

| Component | Status | Notes |
|---|---|---|
| **Decisions** | | |
| ADR-E SDK shape, manifest, `.easyext` | in-progress | drafted as [0013](../decision/0013-extension-sdk-shape-manifest-easyext.md), Proposed |
| ADR-F WASM logic layer + runtime | done | [0014](../decision/0014-wasm-logic-layer-chicory.md) Accepted 2026-09-24 after the on-device spike |
| ADR-G SDK licensing (Apache-2.0 proposal) | done | [0015](../decision/0015-extension-sdk-licensing-apache.md) Accepted; 2026-09-24 amendment moves the validation core (parser, schema validator, layout, JCS, signatures) to Apache-2.0 `services/shared/extension-schema` (`:extension-schema`); the runtime stays under 0008 |
| ADR-H Registry + signing model | in-progress | drafted as [0016](../decision/0016-extension-registry-static-index-ed25519.md) |
| ADR-I LSP client + server lifecycle | in-progress | drafted as [0017](../decision/0017-lsp-client-hand-rolled-server-lifecycle.md) |
| Design doc set (arch, sdk-reference, HLD, LLDs, threat model, rules, tests) | done | cross-checked for consistency 2026-09-24; map in [arch.md](arch.md#document-map) |
| ux-overhaul ADR-B editor engine | done | [0018](../decision/0018-editor-engine-and-decorations.md) |
| **M0 prerequisites** | | |
| Command registry + keymap | in-progress | see ux-overhaul tracker; one `KeyBinding` model with two-press chords (`prefix`), `when` expressions and `args`, layered built-in < extension < keybindings.json, `ChordDispatcher` honouring `when` at both steps |
| Settings schema + layering | in-progress | global layer only |
| Editor decoration layers (squiggles, inlay, popups, gutter) | in-progress | PLT-05: model, painters, geometry, `EditorPopup` + JVM tests landed (`workspace/decor/`); inlay = end-of-line ghost text, code lens = gutter glyph (0018); token overlay stays in the highlighter; instrumented screenshot checks not run (no device) |
| **M1 no-server language features** | | |
| language-configuration.json bundled with grammars | done | 57/229 languages from VS Code 1.139.0 (MIT), generic fallback for the rest |
| Brackets, auto-close, surround, comment toggle, indent/onEnter rules | done | basic: auto-close, overtype, surround, enter/indent rules, comment toggle (`editor.action.commentLine`, Ctrl+/), matching bracket; pair colourization not done |
| Snippets + word completion | not-started | |
| **M2 LSP core** | | |
| JSON-RPC transport + path mapping + doc sync | in-progress | `:lsp` core done; app wiring done (`app/lsp/`: every port, `ServerRegistry` for `lsp.servers` + contributed `languageServers` of enabled packs (`ContributedServers.Provider`: own environment only, `lsp.spawn` granted, `${extensionPath}`/`${config:}` expanded, `sandbox.install` steps whose `when` holds + `verify` as the install recipe); `workspace/lsp/`: tab lifecycle, per-language settings). On-device proot pipe test with a real server not run yet |
| Server lifecycle, memory budget, crash backoff | in-progress | done in code: `ProcMemoryProbe` finds the proot child of the app in `/proc` by argv + project bind and sums the tree's VmRSS; `onTrimMemory` mapped to `MemoryPressure`; status item with restart/stop/start/log; install notice runs the pack recipe in a terminal. Not yet measured on a device |
| Python end to end (diagnostics, completion, hover, definition) | in-progress | M2/M4 presenters built on decision 0018 layers: diagnostics + Problems, completion (snippets, resolve, commit chars), hover, signature help, definition family, references, rename preview, code actions + lightbulb, formatting (document/selection/on-type/on-save + codeActionsOnSave), highlights, inlay hints, `@`/`#` symbols, Outline. `lspReady:`/`lspState:`/`lspSupports:<lang>:<feature>` context keys published to extensions. Needs a device run with a server installed |
| **M3 declarative extensions** | | |
| Manifest loader + contribution registry | in-progress | :extensions core done. Wired into :app (`app/extensions/`): inventory over SandboxPaths + APK built-ins, SettingsPort over layered settings, live adapters for commands/palette, keybindings (keymap extension layer under keybindings.json), menus (editor/title, editor/context, editor/touchToolbar, explorer/context, commandPalette, keyRow), status bar items, key rows, snippets, languages/grammars/language-configuration, `configuration` -> SettingsRegistry, colour themes (`workbench.colorTheme` by label or `<extId>/<themeId>`, JSONC + relative `include`, `uiTheme` base palette, applied to the Material colour scheme and editor/syntax colours, picker cards; icon themes still listed only). Descriptor cache (`.descriptor.json`) not done |
| Action vocabulary | in-progress | App HostPort done: terminals (named + command PTY), sandboxExec capture with separate stderr, editor edits/snippets/openFile, prompts, stages, built-in commands, tasks.json. `lspRequest` routes through `LspRequestGateway` (`textDocument/*`, `workspace/symbol`, `workspace/executeCommand` only; best ready server offering the feature; caret position params by default; `then` shows locations, applies `TextEdit[]`/`WorkspaceEdit`, or shows the result); WorkspaceEdit resource operations refused for `applyEdit` |
| Extensions screen + install from folder/.easyext | in-progress | list/state/toggle/crash re-enable/safe mode exit/inspector/capabilities/log, local install with capability sheet and atomic `current` flip; disable (toggle), uninstall, and rollback of local installs ("Roll back to <version>" in the detail view: re-validates the retained version, re-asks for capabilities never approved for it, keeps the newer version for roll forward, `state.json` records `previous` backward-compatibly; revocation check is a seam until M6); packs with `sandbox.install` steps install with their setup commands and server commands shown verbatim on the sheet, and the steps run later in a visible workspace terminal from the missing-server notice (install-time verify/rollback of steps stays with the M6 registry installer) |
| First-party packs shipped as extensions (Python, themes) | done | built-ins load through the package reader: `easyide.core-snippets`, `easyide.git-commands`, and the declarative productivity packs `easyide.tablet-toolbar`, `easyide.key-rows`, `easyide.toggles`, `easyide.git-extras`, `easyide.project-tasks` (JVM-checked: every command/menu/key/status reference resolves, when-clauses parse, written settings accept their values, shell lines pass `sh -n`), and `easyide.themes` (original Ember Night dark and Morning Linen light, held to the built-in palettes' WCAG checks; applied via `workbench.colorTheme` and the theme picker); **`easyide.python`** (2026-09-24): grammar, language configuration and Python snippets moved out of the app's bundled assets into the pack (core index now 228 grammars; `tools/build-grammars.py` writes Python into the pack), pyright + ruff servers with a setup recipe, Run File, interpreter picker, status item. Built-in packs now declare their servers in every environment. `PythonPackTest`: the pack zipped and installed as `.easyext` parses to the same descriptor and declares the same servers and recipe |
| **M4 language packs + customization UI** | | |
| Additional language servers | done | built-in packs `easyide.typescript`, `.go`, `.cpp`, `.shell`, `.web` (HTML/CSS/JSON), `.yaml`, `.markdown`, `.rust` (rustup, opt-in by setup): setup/verify/uninstall recipes, no bundled binaries; versions pinned for Ubuntu 24.04's Node 18; licences checked 2026-09-24 (Apache-2.0: typescript-language-server, typescript; MIT: bash/yaml servers, vscode-langservers-extracted, pyright; apt: gopls, clangd). `tsx`/`jsx` sent as `typescriptreact`/`javascriptreact`. Not run against real servers yet |
| Customization UI (hide/reorder contributions, server overrides, custom servers, profiles, safe mode) | in-progress | Done (UI + JSON): `workbench.contributions.order` (P, OBJECT merge; `ContributionOverrides` in `:extensions` applied to menus, `statusBar.left/right`, `keyRows`), Extensions inspector Hide/Show + Move up/down (user layer, whole-list writes; `NON_HIDEABLE` enforced at runtime, refused in UI, warned in JSON); `keyRows.layouts` (G, replace-by-id, bad rows reported) + `keyRows.active` picker; Language servers screen (pack + custom servers, source/languages/command/on-off, add/edit/remove custom, per tab layer, ignored `lsp.servers` entries and bad fields shown, also as JSON-editor warnings); Keyboard shortcuts screen (built-in/extension/user with source, conflicts incl. contributed `when` overlap, add/remove via keybindings.json); JSON editor key/enum/command completion. Deferred: `workbench.stages.placement` (contributed stages are not rendered yet), prefix/reserved-chord conflict kinds, rebind/reset actions |
| Semantic tokens, code lens, inlay hints | in-progress | semantic tokens: `TOKEN_OVERLAY` advertised with the `SemanticRules` types/modifiers; full/delta (range first above 2,000 lines) for the visible document after edits, on open and on refresh; per-line `SemanticOverlay` shifted onto newer text, painted after the TextMate spans (`SemanticPaint`, O2 pinned at the `AnnotatedString` level); theme/customization selectors via `SemanticStyler`; `editor.semanticHighlighting.enabled` per language (true/false/`configuredByTheme`). Code lens: advertised through the gutter layer (0018), lazy resolve for visible lines, gutter tap lists the line's lenses, `editor.action.showReferences` opens References, other commands `workspace/executeCommand`; `editor.codeLens` per language. Inlay hints: `inlayHint/resolve` and tap-to-accept not done (needs a hit target on ghost text in `EditorPane`) |
| Themes + colour customization | in-progress | `workbench.colorCustomizations`, `editor.tokenColorCustomizations` (`textMateRules` collapsed per property, `roles`, VS Code shorthands), `editor.semanticTokenColorCustomizations` (`rules`, `enabled`), each with `[Theme]` blocks, merged by `ThemeCustomizer` in the 8.3 order; role `fontStyle` painted by the highlighter; `ThemeTokens` carries semantic rules + the theme's `semanticHighlighting`. Theme-file `fontStyle`/`semanticHighlighting` are not read by the theme file reader yet, and `EasyIdeTheme(themeLabel)` still needs `workbench.colorTheme` passed in |
| Icon themes | done | `workbench.iconTheme` + Settings picker; `ActiveIconTheme`/`IconThemeFile` (fileNames > multi-part extensions > languageIds > file; folder names; `light` section), PNG via `BitmapFactory` + LRU (`SettingsPolicy.ICON_CACHE_ENTRIES`); every file row (explorer, quick open, Recent, Search, source control) uses it through `LocalFileIcons`/`FileIcon`. SVG via the in-house subset renderer at the row's pixel size; font icons skipped and logged. Built-in pack `easyide.file-icons` (default theme, generated by `tools/build-file-icons.py`, [ADR 0028](../decision/0028-file-icons-drawn-in-house.md)); picker preview strip; [icon-themes.md](icon-themes.md) |
| Safe mode and per-language `lsp.enabled` for servers | done | `ServerGates`: safe mode disables `lsp.servers` entries without an extension; a server keeps only languages whose `lsp.enabled` resolves true and is disabled when none remain |
| **M5 SDK tooling** | | |
| `easyide-ext` CLI | in-progress | `tools/easyide-ext` (own Gradle build, compiles the shared SDK core): `init` (all seven sdk-reference templates: theme, snippets, language-pack, lsp-pack, toolbar-command, wasm-rust, wasm-assemblyscript, each with passing `test/` scenarios; WASM templates built with their toolchains in tests), `validate` (app pipeline + full WASM static check + `--strict` readiness), deterministic `package`, `keygen` (PBES2-encrypted Ed25519, `--rotate`), `sign`, `verify`, `publish`, `registry build`, `test` (L1 scenarios with the app's ActionRunner; WASM commands not executed), `dev` (adb push + DEV_RELOAD broadcast, `--local` request file, `--watch`); `--json`, exit codes 0/1/2; 25 JVM tests. App side of `dev`: see the next row. Template rendering and ignore rules now live in the shared `dev.easyide.extensions.authoring` (used by `init`/`package`/`dev` and the app) |
| In-app dev loop (create, install from folder, live reload, log) | in-progress | 2026-09-24, `app/extensions/dev/`, `app/extensions/authoring/`: `extensions.developerMode` (G, default off, generic Settings row); `DevReloadReceiver` for `easyide-ext dev` over adb (exported, `DUMP`-protected; inbox `<external files>/dev-inbox/<id>.easyext`); `.easyide/dev/<id>.json` watcher on the open project for `dev --local` (folder mapped into the project, escapes refused, request deleted once read); `DevInstaller` stages through `LocalInstaller` as `Source.DEV` under `<version>-dev.<n>`, capability sheet on first install / replacing a non-dev copy / capability change, silent reload otherwise, toast + Extension Log line per outcome; "Create extension" on the Extensions screen (theme, snippets, language-pack, lsp-pack, toolbar-command from APK assets copied at build from `services/shared/extension-templates`; written to `<project>/<name>/`, mirrored to a linked folder) then "Install from this folder" (developer install when developer mode is on). Extension Log and contribution inspector were already there. 18 new JVM tests. Not done: device checks (DUMP gate, adb-pushed file readable on API 30+, < 5 s reload), a system notification for a waiting sheet, the WASM templates in-app, deactivate/reactivate of an active WASM extension on reload (runtime keeps state by id) |
| Samples + author docs | in-progress | `author-guide.md` quick start matches what exists (CLI commands incl. `test`/`dev`/`publish`, Create extension, developer mode, dev reload behaviour, Extension Log); samples: `services/shared/samples/wasm-word-count` (Rust) and `-as`; every template is a runnable sample (`init` golden tests, `ExtensionTemplatesTest`). Not done: tutorials 2-4 still describe some files the templates do not generate (`l10n/`, `icon.png`), samples in CI, a timed "theme + snippet pack in < 15 min" run on a device |
| **M6 registry** | | |
| Static signed index repo + browse/install UI | in-progress | shared core done (`dev.easyide.extensions.registry`, Apache-2.0): strict `IndexEntry`/`PublisherKeys`/`Revocations` parsing, `RegistryVerifier` (root-signed index + revocations with anti-rollback, publisher file, entry signature with active unrevoked key, TOFU pin with rotation chain, revoked versions, size+sha256), `Catalog` (latest compatible unrevoked, search ranking, filters), `RegistrySigning`; CLI `publish` (signed entry to a branch of the index repo via system git, `--register-publisher`, `--url`/`--package-base`) and `registry build` (verifies every entry, root-signs index, publishers, revocations), round-trip tested against a local bare repo. App side (2026-09-24, `app/extensions/registry/`): `extensions.registries` (default none; no public index yet) and `extensions.autoCheckUpdates`; `RegistryClient` (https-only `HttpURLConnection` port, ETag/304, `RegistryPolicy` caps and timeouts, staging + verify + anti-rollback + rename swap into `extensions/registry/<id>/`, `meta.json`, lazy verified publisher files, disk copy re-verified on load); `TrustStore` pins in `state.json` written on commit, rotation re-pins, forget; `RegistryInstaller` (engines, cache `<sha256>.easyext` via `.partial`, `checkBytes`, `trustEntry`, local stage/validate, entry-vs-manifest id/version/scope/capabilities check, commit as `REGISTRY` with signer, pin); revocations disable installed versions after refresh (never uninstall) and feed `LocalInstaller.isRevoked` (stage and rollback); update badge only; Browse tab (search, registry badge, version, capabilities, detail with Install/Update/forget pin, refresh, "no registry configured", index age, stale and error reasons), install sheet says "registry <id>, signed by <keyId>"; Ed25519 on Android is Tink `Ed25519Verify` (0016 amendment). 14 JVM tests incl. offline cache install with the network port throwing, tampered package/index, revoked key, revoked rollback, anti-rollback, rotation. Not done: capability-delta diff sheet and CHANGELOG on update, install journal/`recover()`, `sandbox.install` run/verify at install, sideload `.easyext.sig`, developer-mode gate on forget-pin, device test of the < 10 s offline install |
| Open VSX secondary source | not-started | skipped in the M6 app pass (2026-09-24); `extensions.openVsx.enabled` not declared yet |
| **M7 WASM layer** | | |
| Runtime embed, ABI v1, host API, capability enforcement | in-progress | guest bindings: `easyide-guest` Rust crate and `@easyide/guest-as` (services/shared, Apache-2.0); `init` templates `wasm-rust` and `wasm-assemblyscript` (bindings vendored at CLI build); samples `services/shared/samples/wasm-word-count` (Rust) and `-as` (AssemblyScript) run in the real `WasmHost` in `WasmSampleTest`; optional `_initialize` export (reactor convention) validated and called under limits; app hosts WASM (2026-09-24): `:app` depends on `:ext-wasm`, `app/extensions/wasm/WasmRuntime` is the runtime's `LogicHost` and WASM `Activator` (activation events, deactivate on disable/uninstall/leaving the env, `onTrimMemory`, `extensions.wasm.enabled`, crash loop -> existing crash-disable), every host port bridged to app services (editor/lsp/processes via `WorkspaceBridge`, files with symlink-escape refusal, prompts via the L1 prompt host, dynamic status items, layered config with L1 write rules, `HttpURLConnection` net with address/redirect/size rules, JSON-file storage, clipboard; secrets return null), workspace/config events, WASM completion + hover merged after LSP; `WasmRuntimeTest` runs the Rust sample's command through `ExtensionsRuntime` -> `LogicHost` -> `WasmHost` ("4 words") and a denied capability as `E_CAPABILITY`. Not done: contributed views are not rendered (view data is stored), `editor.decorate`, `lsp.notify`, `fs.watch` of changes made inside the environment, extension secrets UI, a device run | `:ext-wasm` host on Chicory landed in b322368 after the on-device spike passed (ABI v1, capability-gated host functions, metering and limits); this row was not updated then |
| **UI contribution points (R4)** | | |
| Navigation, containers, views, documents, openers, presets, badges | in-progress | manifest parsing and validation (`E_VIEW_*`, `E_UI_*`, `E_ICON_RULE`), `manifest.schema.json`, capability `ui.contribute`, fixtures and negative tests for every limit; runtime stores; `${arg:}` and `openDocument`; `viewData` kind `object` with `intervalSec`; authoring guide [views.md](views.md), [ADR 0027](../decision/0027-extension-view-schema.md). Old packs parse and behave as before (fixtures unchanged). Not done: `terminal` component, provider visibility events, a device run |
| `easyide-ext` validate / test for views | done | validate reports view and icon problems against the file that has them; `expect.views` renders a view or document with the app's planner (`ViewScenarioTest`) |
| Sample packs Docker, Agents, Chat | in-progress | `services/mobile/app/src/main/assets/extension-samples/`, optional; Chat guest source in `services/shared/samples/chat-guest-as` |
| **M8 Node host (conditional)** | | |
| vscode API subset in sandbox | not-started (by decision) | [m8-gap-analysis.md](m8-gap-analysis.md) (2026-09-24): none of the top 60 Open VSX extensions needs the Node host itself; what blocks the rest is DAP, notebooks, webviews/sign-in and container daemons, none of which L3 provides. Not building; next are L1 gaps: formatter contribution, test explorer, Open VSX subset import |
