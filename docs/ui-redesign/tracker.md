# Feature: UI Redesign - Tracker

Status legend: not-started / in-progress / done / blocked

Design: [arch.md](arch.md). Design accepted by the owner on 2026-09-24 (ADR 0025, 0026 Accepted). Rows below track the build; nothing under R0 onward is built until marked.

| Component | Status | Notes |
|---|---|---|
| **Decisions** | | |
| ADR 0025 shell model | done | Accepted 2026-09-24 |
| ADR 0026 identity, kit, properties | done | Accepted 2026-09-24: orange default accent, Block motif |
| Extension-track hand-off (SDK additions) | done | implemented locally in R4 (the cloud session was not available): sdk-reference, validate, test, runtime; [ADR 0027](../decision/0027-extension-view-schema.md) |
| In-flight worktree plan (salvage / merge / remove) | done | reliab, editor, git merged; layout pure logic salvaged (`ui/layout` screen edits dropped); `ui/home` stale, removal pending; `WorkspaceViewModel` split under 600 lines |
| **R0 Foundations** | | |
| Import-ratchet test + allowlist | done | `ImportRatchetTest`; allowlist `ui-ratchet-allowlist.txt` = 9 files, 29 imports (was 55 files, 202 imports before the shell hosted Home, Settings and Extensions, then 29 files, 87 imports; the workspace panels, git, LSP, extension prompts and stock dialogs left it in R3, what remains is Diagnostics, New project and Onboarding); fails on growth, new offenders and un-lowered improvements. ThemePicker's dynamic colour-scheme imports are not on the banned list |
| `tools/ui-lint.sh` (colour, type, copy, motion rules) | done | 12 rules (R5 added icon imports outside the resolver, resting shadows, icon-in-circle, forbidden copy words, Title Case strings), 0.3 s, baseline `tools/ui-lint-baseline.txt` (1 entry: the dirty dot of the document strip); not yet wired into CI (no test workflow exists) |
| Roborazzi compatibility spike | done | works on AGP 9.3.1 / Kotlin 2.4.10 / compileSdk 37 with three settings (kit.md section 7); Geist loads; Roborazzi Apache-2.0, Robolectric MIT; gallery golden in `app/src/test/screenshots`; Dropshots fallback not needed |
| `Appearance`, `UiMetrics`, `Motion`, `Feel` + settings schema | done | `ui/props`, `AppearanceSettingsSchema`; defaults equal today's constants (test); wired into `EasyIdeTheme` (accent, scale, shapes, typography, motion) |
| Token refactors 1-10 (kit.md section 6) | in-progress | done: 2 radius scale, 3 shapes fn, 5-6 typography by pairing, 7 theme takes `Appearance`, 8 `withAccent`, 10 (follows accent); open: 1 spacing call sites, 4 control scale call sites, 9 semantic accessors + `Tone` |
| Kit primitives (about 20) + gallery | in-progress | all 17 primitives landed (see kit.md 3). Gallery landed: `ui/kit/gallery` (debug and canary only), opened from Settings > Diagnostics > Developer tools, live controls for density, corners, font scale, accent, theme mode, motif and reduce motion; release has a no-op twin (`src/release`); checked on the built APKs: the route string and the gallery classes are in the canary dex and mapping, and absent from the release dex, mapping and resources (AGP creates no release unit-test task, so this is a build-output check, not a unit test). Semantics tests (role, name, state, 44dp) and gallery smoke tests run under Robolectric in `src/testDebug`. `tools/ui-device-check.sh` for the on-device 44dp and label check. Not seen on a device by the author; the screenshot goldens of the gallery matrix are still open |
| **Density (density.md)** | | |
| Tokens: dense step, auto density by width class, control scale, `Kit.text` type roles | done | `Density` DENSE/COMFORTABLE/SPACIOUS plus `DensityPref` (auto, stored `appearance.density`, `compact` decodes as `dense`); `UiMetrics.of(appearance, width)` with `control.{rowHeight, sectionHeaderHeight, toolbarButton, hitBox, rowIcon, railWidth, railIcon, bottomBarHeight, tabHeight, panelTabHeight, statusHeight, fieldHeight, buttonHeight, tagHeight, indent, hPad}`; `touchFloor` is 44 on COMPACT and 40 on MEDIUM/EXPANDED and the theme installs it as Compose's minimum touch target; `KitText` roles (display, heading, title, body, caption, label, mono, monoSmall); all screens migrated by `tools/codemod-type-roles.py`. JVM-tested |
| Kit primitives to the row anatomy | done | one-line `KitRow` (level, twistie, actions, secondLine), `KitSectionHeader`/`KitSection` (count, actions, collapsible, flat), `KitTwoColumnRow`, content-sized scrolling `SegmentedTabs`, `kitHitSlop`, dialog actions in a `FlowRow`, banner inside the safe area. Screens still call the old shapes (subtitle now inline, no second line by default); migrating call sites (settings rows to `KitTwoColumnRow`, trees to `level`/`twistie`) is open |
| Home, Extensions, onboarding, New project, Diagnostics on the density kit (agent DENSE-HOME, r8/home) | done, not seen on a device | Home panel: flat collapsible sections with counts (Resume, Running, Environment, Projects), one-line project rows (folder glyph in the tile colour, name, branch and location inline, changed-file tag and age at the end), search and sort on one line at the field height, Resume as a two-row block; stage `HomeNowPage` is two aligned columns on EXPANDED with a projects summary; `ProjectPage` details as two-column rows, recent files as name + folder rows, actions on one line. Extensions: one-line rows (`name  id@version`, state or source tag, switch), tab strip at the panel-tab height, page header compact, Contributions as twistie groups with sub rows, Capabilities as two-column rows, sections with counts. Onboarding, New project, Diagnostics: gutters from `Kit.control.hPad`, step pane top-aligned, rail 32%, Diagnostics facts as two-column rows. Roborazzi goldens (Home, Extensions, onboarding) at EXPANDED/Dense 1152, MEDIUM/Dense 720, COMPACT/Comfortable 411 read and fixed; extension Browse rows and New project / Install Linux / Diagnostics have no golden; no check at 320dp or font scale 2 beyond the layout rules |
| Shell chrome heights | done | rail 52 (labelled +16 on EXPANDED), bottom bar 56, doc tabs 34, status 24, panel tab rows 30, from the tokens; not seen on a device |
| Density gallery + goldens | done | `AnatomySection` and `TypeSection` at Dense and Comfortable side by side; three Roborazzi goldens; the placeholder golden was re-recorded (title style). Semantics tests per width class (`KitSemanticsCompactTest`, `KitSemanticsWideTest`) |
| Workspace on the density system (R8 ws) | done | file tree, source control, search, problems/references/outline, rail, document strip, panel headers and tab rows, terminal bar and key row, find bar, welcome, status strip on `Kit.control`/`Kit.text` and `KitRow`: tree row is `KitRow` (twistie, 16dp icon, name, git letter at the end, hairline guides under each ancestor twistie), source control is commit field, full-width Commit, then collapsible sections with counts (`panelSection`) and one-line change rows whose stage/discard actions show on the selected or hovered row (and in the long-press menu); the rail is icon-only with a bottom cluster (Extensions, Settings, Commands, Projects, Close), labels stay on the bottom bar (`shell.navigation.labels` = always still labels a rail); panel tab rows share one `PanelTabRow`; `KitBanner` gained `lines` and clamps its buttons to the row token, `KitButton` gained `fillWidth`. Goldens: `WorkspaceGoldenTest` records 8 shots at expanded/dense 1152, medium/dense 720, compact/comfortable 411 and a 320dp font-scale-2 stress case (`src/test/screenshots/workspace`), read and corrected. Not seen on a device; rail labels are semantics only (no hover tooltip yet) |
| Settings on the density system (panel, pages, rows) | done | flat category panel (`KitSectionHeader`, one-line `KitRow` per page with the block marker, search at `fieldHeight`, results as `Title  Category`); every page row is one `SettingLine`: `KitTwoColumnRow` on a wide page (label column, right-aligned control column, text fields fill it, modified dot in the start gutter, layer badge and "overridden" as a note under the row), the one-line `KitRow` with a second-line description on a phone, stacked when the row is narrow for its text size (`RowLayout`, decided once per page by `SettingsPageFrame` from the width the rows get and the font scale); sections are collapsible `KitSection`s with a count; theme cards in equal-width columns (`themeColumns`); Keyboard shortcuts, Language servers (tag and actions in the row), Layout, Sandbox, Advanced, Git credentials on the same anatomy; no hard-coded padding left in `ui/screens/settings`. Roborazzi goldens in `app/src/test/screenshots/settings`: panel, panel searching, Appearance, Editor, Keyboard shortcuts, search results at 1152dp Dense, 720dp Dense, 411dp Comfortable and 320dp at font scale 2.0, plus a no-horizontal-overflow assertion at all four. Read by the author from the recorded PNGs; not seen on a device. Language servers, Layout, Sandbox, Advanced and the JSON editor are migrated but have no golden (they need the view model or the shell host) |
| Editor text area to VS Code metrics (agent EDITORTEXT, r11/editortext) | done, not seen on a device | default `appearance.fontPairing` is System (Geist and Geist Mono stay selectable); editor font is the platform `monospace` (Droid Sans Mono on Android) at 14 with line height 1.35 x size, ligatures off (`codeTextStyleOf`); new `editor.*` settings in `EditorSettingsSchema` (`fontFamily`, `lineHeight` as 0 / multiplier <= 8 / sp, `letterSpacing`, `cursorStyle`, `cursorWidth`, `lineNumbers`, `renderLineHighlight`, `matchBrackets`, `guides.indentation`, `scrollbar.vertical`, `wordWrap`) shown in Settings > Editor and honoured live; gutter = 18dp glyph margin (14dp under 360dp) + right-aligned numbers drawn from the text layout (3 digits, 2 when narrow) + one character gap, so wrapped lines number correctly; caret drawn by `drawCaret` (2dp line, block or underline, 500 ms blink restarting while typing, neutral `editorCursor.foreground`, no longer the accent); bracket match is a faint fill with a 1dp square outline, current line a subtle band with optional border, find current match outlined, indent guides, 10dp fading overlay scrollbar; completion/hover/code action popups 22dp rows, 2dp radius, 1dp border, no shadow. Goldens in `app/src/test/screenshots/editor` at 1152dp Dense, 411dp Comfortable and 320dp at font scale 2.0, dark and light (bracket + squiggle, selection + find, completion, hover, word wrap + relative numbers + block caret). Existing goldens that render the default font pairing (settings, home, extensions, onboarding, kit gallery) are stale until re-recorded |
| `ui-device-check` histogram + alignment | done | fixtures self-test only (`--dump`); not run on a device |
| Motif drawables (cursor block, crop corners, prompt glyph, cell fill) | done | drawn, not typed (Geist Mono has no U+25AE); compiles and the logic is unit-tested, not seen on a device; the 6-cycle blink cap for Home and the 600ms reset after input landed in R5 (`BlinkClock`) |
| **R1 App shell** | | |
| `ShellState`, registries (navigation, container, document) | done | pure engine in `ui/shell`, unit-tested; app scope is wired to it through `ShellViewModel` (`ui/shell/host`); `ShellAction.ResizePane` added for dragged panel widths; workspace scope is not wired (R3) |
| `NavSurface` (bottom bar / rail), `PanelHost`, `StageHost` (1 group) | in-progress | built and wired for app scope: bottom bar (max 5 + More via `KitDialog`) or rail per size class and `shell.navigation.*`, badges, docked primary panel with a persisted draggable width, one-group stage (tab strip wide, titled page with back on compact), `AdaptiveScaffold` insets and IME, `ShellHost`. JVM logic tests only; not seen on a device. Open: overlay side/bottom sheets for workspace panels (R3), secondary/bottom panels, more than one group, Ctrl+Alt+n nav shortcuts |
| Document URIs + open/preview/pin/history | in-progress | `DocumentUri`, `DocumentRegistry` (open-with, placeholder), `EditorGroup`/`EditorStage` (preview, pin, MRU, history, 1-4 groups) done and tested; no UI (`StageHost`) yet |
| Home "Now" page + project page | in-progress | built on the kit in `ui/screens/home`: `HomePanel` (resume hero, Running, Environment, Projects with search/sort/row menu), `HomeNowPage` (stage), `ProjectPage`, all Home dialogs on `KitDialog`/`KitField`; bound in the shell (`ui/shell/host/HomeBinding.kt`): the panel on every size class, `HomeNowPage` as the stage default on a wide window (the panel then drops its Now sections), `ProjectPage` as `easyide://project/<id>` titled with the project name, `HomeDialogHost` mounted once at the shell, Stop wired to `HomeViewModel.onStop`. `HomeScreen` is deleted, and with it `detailOpen` / `onCloseDetail` (the shell owns selection). Running reads the workspace registry (one row per live workspace, not per terminal), LSP statuses with RSS, and provisioning environments (no percentage source, so an indeterminate sweep). Compile, JVM logic tests and R8 release build only; not seen on a device. Open: per-terminal and agent rows, install percentage, environment size, last commits on the page, cursor blink cap |
| Extensions on the shell (list + extension page + install flow) | in-progress | built on the kit: `ExtensionsPanel`, `ExtensionPage`, `ExtensionsDialogs` (install approval, rollback, create, Browse detail, uninstall confirm) with unit-tested row/filter/grouping/capability-diff logic; bound in the shell (`ExtensionsBinding.kt`): panel with its own single header row, `ExtensionPage` as `easyide://extension/<id>` titled with the extension name, `ExtensionsDialogs` mounted once; the page's Settings link opens `easyide://settings/extensions#<id>` through the shell. `ExtensionsScreen` is deleted. Compiles and passes JVM tests; not seen on a device |
| Settings on the shell (categories + pages + Appearance + Layout) | in-progress | built on the kit, not seen on a device: `SettingsPanel` (search, layer control, safe-mode banner, categories or results) and `SettingsPage(category)` (generic schema rows with modified dot, layer badge, reset, protected and per-language handling, search results page), new Appearance page (live preview, accent swatches and hex with contrast and hue feedback, all `appearance.*` rows) and Layout page (`shell.*` keys in `ui/props/ShellSettingsSchema`, up/down reorder, hide, pin, panel placement); Keyboard, Language servers, Sandbox, Advanced are pages; all Settings dialogs on `KitDialog`. Bound in the shell (`SettingsBinding.kt`): panel (now with its own title row), `SettingsPage(category)` as `easyide://settings/<category>` titled with the category name, Appearance as the stage default on a wide window, `SettingsHost.onNotify` shows as a shell toast, `LayoutCatalog.of(navigation, containers)` built from the shell registries (extension packs named through `packOf`); `easyide://settings/extensions#<id>` marks and scrolls to the first row of that extension (`SettingsLink`). `ShellSettingsSchema` is one object (`ui/props`) registered once in `SettingsSchema.all` (the `shell.*` block of `data/settings` is gone); `SettingsScreen` is deleted. Open: no screenshot goldens, the search-result scroll is unverified on a device |
| Back behaviour + app-scope restore | done | `BackHandler` in `ShellHost` runs `BackNavigation`; the snapshot is restored on first window and saved debounced (400 ms) in the app preferences file. Note: on a phone at Home the first Back moves focus to the bottom bar (section 11 step 5) and the second leaves the app. Not verified on a device |
| **R2 Flows and dialogs** | | |
| Onboarding, New project, Install Linux on the kit | in-progress | built on the kit and JVM-tested (layout choice, step states, bounds at tablet sizes, every install state rendered); onboarding is a stepper bar plus a rail beside the step on expanded windows and phone landscape; New project and Install Linux are a page on compact and a centred sheet-style panel on wider windows (a true overlay over Home needs a route change in `AppNavHost`/shell, not done). Baseline profile selector unchanged ("Get started"); not seen on a device |
| 22 non-workspace dialogs to `KitDialog`; banners; toasts | in-progress | Shell toast landed (`Toasts.kt`: `ToastQueue`, `ToastHost` on `KitBanner`, one at a time, 3 s, queue capped at 4, announced by the banner's live region; reached through `ShellActions.notify`, used by Settings). Home's six dialogs (rename, duplicate, delete with typed name, change environment, clone, import) plus the Install Linux and stop-session prompts are on `KitDialog`; Home confirmations are an inline `KitBanner` (no toast primitive yet); Settings and Extensions dialogs open; Diagnostics dialogs (cleanup confirm, crash recovery) on `KitDialog`, Diagnostics messages through the shell `ToastHost` |
| **R3 Workspace shell** | | |
| Editor groups (1-4), document switcher / tab strip | in-progress | `StageHost` draws up to `groupCapacity` groups (1 phone, 2 medium, 4 expanded) along the stage axis: document switcher (title, count, MRU list, close) on a phone, `DocumentStrip` with dirty dot, split and close-group buttons on a wide window; a touch focuses a group. Files are `file:///workspace/<path>` documents rendered by the existing `EditorPane`, kept in step with the view model's tabs by `FileTabSync` (JVM-tested). Open to the side, the tab menu (with close others, also as a long press on the phone switcher) and the git diff and commit documents landed in R6; tab drag is still open (gestures, blocked on PE1). Not seen on a device |
| Panels: Files, Search, Source control, Problems, Outline, Terminal | in-progress | `WorkspacePanels` binds the existing tree, source control, language and terminal panes through `PanelRenderer`s reading `LocalWorkspaceEnv`; Search is a new file-name search (no text search engine exists); Extensions and Settings are containers in the workspace with their pages as stage documents. Phone: side panels are sheets with a scrim (88%, max 360dp), the bottom panel rests on peek / half / full and never goes below 160dp; wide: docked, resizable and persisted per arrangement, bottom 25-70%. Book and tabletop split at the hinge. Not seen on a device |
| Bottom navigation in workspace + input dock | in-progress | the app shell's `NavSurface` with workspace items (Files, Search, Git, Terminal, then More: Problems, Outline, Extensions, Settings, Commands, Projects, Close) and extension items via `NavItemSource`; hidden while the keyboard is up (`NavRules.barShown`); `InputDock` shows the touch toolbar and key rows (terminal row moved out of `TerminalPane`) above the keyboard by `DockRules`, hidden with a hardware keyboard. Not seen on a device |
| Status strip, layout presets, foldable postures, persisted layout | in-progress | `StatusStrip` fits parts by measured width and priority (`StatusPlan`), phone drops project and line count; `LayoutPresetPicker` (palette: Layout: choose a preset) applies auto / focus / classic / workbench / terminalFirst / book / tabletop; layout, sizes, preset and non-file pages persist per project in `SessionSnapshot.shell` and restore tolerantly. No keybinding for the picker or split yet (Ctrl+Alt+P, Ctrl+\ are palette-only). Not seen on a device |
| Workspace stock dialogs/menus to the kit | done | unsaved changes, external-change conflict, delete (a folder asks for its name), name prompt, git confirmations and credential dialog, LSP rename, preview, server question and log, extension prompts on `KitDialog`; explorer, extension and sync menus on `KitMenu`; `ChromeControls.kt` deleted. `MenuModel`, `StatusItems`, `KeyRows`, `ActiveKeyRow` and their tests are unchanged. Compile and JVM tests only; not seen on a device |
| **R4 Extension UI** | | |
| Registries fed by the runtime (navigation, containers, documents, openers, presets) | in-progress | `:extensions` stores for the five points with the `ui.contribute` grant (`RegisteredExtension.uiGranted`), enable/disable, safe mode and per-environment scope from the existing enablement; `ShellContributions` (app adapter, pure) maps them to the engine's specs (bare `activitybar` becomes a sidebar container plus a navigation item); `ExtNavSource` implements `NavItemSource` (spent `when`, badges from view data, command targets); `ExtRegistries` folds containers, types and openers into the registries; `ShellViewModel.effective`, `WorkspaceShellModel.setRegistries` and the renderer fallbacks keep the app shell and the workspace in step as packs come and go; `shell.extensions.contribute` switch. JVM-tested; not seen on a device |
| View schema renderer + limits | in-progress | `viewSchema: 1` decoder, catalog and limits in `:extension-schema` (Apache-2.0); `ViewPlan` (pure) + `ViewData`; Compose drawing in `ui/shell/ext` on the kit only (plus a new `KitSlider`): layout, content, data (lazy list, tree, table, log, chat with follow), input, composer; events with confirm, `as`/`before`/`after`, `open`; data hub with rate/size limits; fetch loops only while a view is on screen; pack SVG icons drawn as vectors. Robolectric tests of the surface; not seen on a device. Not built: `terminal` (reserved), slash commands and attachments in `composer`, split diff (R6), a provider `visible` notification |
| User control + layout catalog | in-progress | extension items and containers appear in Settings > Layout with their pack named, obey `shell.navigation.*` (extension order raised above core by the engine), and each pack has an on/off switch; extension presets join the picker and the Layout page. Not seen on a device |
| Sample packs: Docker, Agents, Chat | in-progress | three optional packs under `assets/extension-samples/` (not built in; Extensions, +, Install a sample pack, then the usual capability sheet): Docker (docker CLI through `sandboxExec`, containers/images views, container document, running-count badge), Agents (sessions as project files, `chat` + `composer` wired to `claude -p` or a documented echo agent, changed-files view, preset), Chat (WASM guest on the storage API, global scope, works from Home). Scripts run against a fake docker and a scratch project in JVM tests; nothing run on a device |
| **R5 Identity assets** | | |
| Custom icon set (24 glyphs) + resolver | in-progress | drawn in code by an engineer, not by a draughtsperson, so still needs a design review: `ui/icons` (4 glyph files, `EiIcons`, `iconFor` / `resolveIcon` honouring `appearance.iconStyle`, Material table for the generic verbs and per-glyph fallback). Path data is M/L/H/V/Z on the 24 grid (20 live area, 1.5 stroke, butt caps, mitre joins, 0/45/90 degree segments only), enforced by `EiIconsTest`. Kit, shell nav and shell workspace chrome and the extension command icons go through the resolver; `ui-lint` rule U-ICO-01 fails a Material icon import in `ui/kit` and `ui/shell`. The identity set has no glyph for search, problems, outline, home, add, close, more, run or save, which stay Material by design (identity.md 6), so those tokens resolve to Material under both styles. Gallery "Icons" section and light/dark goldens (`IconSheetGoldenTest`); not seen on a device |
| App icon, splash, adaptive + monochrome layers | in-progress | block-cursor mark (chevron with square terminals + solid block) as adaptive icon with foreground, background and monochrome vectors; debug uses a hollow block, canary an accent background with a graphite mark; monochrome status-bar mark (`ic_stat_easyide`) for the sandbox notification; splash uses an animated vector whose block blinks once, on the theme's window background (`IdentityColorsTest` keeps the XML colours equal to the palettes). Builds (release R8, canary) compile; `lintRelease` reports two pre-existing errors elsewhere and nothing in these resources. Rendered only as a Python sketch of the geometry, not seen on a device or launcher |
| Motion + haptics helpers, empty-state art, copy pass | in-progress | `BlinkClock` (pure: 530/530 blink, 600ms solid hold after input, 6-cycle cap; unit-tested), `CursorBlock` on it (no infinite transition left; a capped cursor stops its timer), Home header and empty-state action cursors capped at 6, `MotifSurface` table (1-3 instances per surface, tested), banner haptics by tone (success confirms, danger rejects). Ten `EmptyArt` variants applied to Home, Extensions, Problems, Source control, Terminal (new empty state), Welcome; 24 command titles moved to sentence case (see chainlog); `tools/ui-lint.sh` gained U-ICO-01, U-AI-05, U-AI-06, U-AI-08, U-TYP-05. Not done: the caret input hold has no caller yet (CursorBlock takes `lastInputMs`, no field feeds it), pane snap and drag pick-up haptics are not wired, art is ASCII not box drawing. Not seen on a device |
| **R6 Split view, diff, gestures** | | |
| Open beside, groups, `git-diff` / `git-commit` documents, diff providers | in-progress | built, JVM- and Robolectric-tested, not seen on a device. Documents: `git-diff:///<repo path>?base=<end>&head=<end>` (`Comparison`; an end is a revision, `index`, `worktree` or `empty`) drawn by `GitDiffDocument` (unified, or side by side when the document itself is 840dp wide; hunk navigation, stage / unstage / discard hunk for the index and working-tree pairs, word-level highlighting, the editor's grammar for colours), and `git-commit://<sha>` by `GitCommitDocument` (message, author, date, parents, files that each open their diff); both restorable, both unavailable-safe. `DiffScreen` (the dialog) is deleted; a change row previews on tap, keeps on double tap, and has a menu (open changes, to the side, open file); a commit in the graph opens its document. Provider seam: `DiffProvider` (scheme, subject, load, hunk actions) and `DiffProviders` in `ui/shell/diff`; the built-in `git-diff` one is `NativeGitDiffProvider`; extension packs cannot register one yet (no `compare` contribution in the runtime, `DiffProviders.register` is where it lands). Git service: `compare` between any two ends and `commitDetail`. Open beside: `OpenBeside` rules (one group on a phone: opens in place with a message; two on medium, book and tabletop; four on expanded), a tab menu (long press or right click; keep, pin, move, close scopes), palette commands `Open to the Side` (Ctrl+Enter), `Split Editor` (Ctrl+\), `Close Group`, `Move Editor Into Next Group` (Ctrl+K Ctrl+Right) in the keymap table; settings and extension pages can split, project pages cannot. Menus now open under the press (`KitPressPoint`, `KitMenu(at)`) for the file menu, the tab menu, the change rows and the editor; a right click on a tree row opens the file menu; new file, new folder and rename are typed into the tree. Not done: dragging tabs between groups, a whole-file stage action inside the diff document (the panel rows keep theirs), closing several dirty files at once asks only for the last |
| Editor chrome (r11): tabs, breadcrumbs, find widget, one-row dock, welcome | in-progress | VS Code structure around the text, verified by Roborazzi goldens (`EditorChromeGoldenTest`, dark and light at 1152 dense, 720 dense, 411 comfortable, 320 at font scale 2), not seen on a device. Tabs: icon (file icon theme), name, dot or cross (cross on the active or hovered tab), 1dp accent top line on the active one, italic preview, hairline separators, active kept in view, chevron menu of open documents while tabs overflow. Breadcrumbs (`breadcrumbs.enabled`, hidden on compact-height windows): path from the project root then the symbols holding the caret (outline, `SymbolRow.span`), clipped from the left; a path crumb opens a menu of the folder (folders drill in, files open), a symbol crumb jumps. Find: compact card at the editor top right, 420dp max, replace row behind the chevron, 22dp flat option toggles, full width on compact windows. Input dock: the text-action row and key row are now one 28dp row (keys, hairline separators, `more` opens the touch toolbar actions), shown only with the on-screen keyboard up and on a compact window or without a hardware keyboard; `workbench.keyRow` and `workbench.touchToolbar` (auto / always / never) in Settings > Editor. Welcome: mark, Start and Recent columns, shortcuts table. Not done: a Clone entry in Start (no clone command exists), a run button in the group actions (no run command), tab drag |
| Gesture layer, caret scrub bar | blocked | waits for the editor engine (ADR 0018 PE1) |
| **R12 Git and explorer (VS Code parity)** | | |
| Menu component | in-progress | `KitMenu` is the VS Code menu: nested submenus, hints, checks, disabled and danger rows, arrow-key navigation, `anchor` placement, 6dp radius; pure placement and focus logic tested (`KitMenuNavTest`). Nested popups and the keyboard not seen on a device |
| Source control pane | in-progress | commit split button (commit, amend, push, sync), status-tinted change rows with hover actions, list or tree, lane graph with curved merges and ref chips, commit context menu (checkout, detached, branch, tag, cherry-pick, compares, copy), compare list. New git operations tested on a temp repository. See [vscode-parity-git.md](vscode-parity-git.md). Extension entries `Add to chat` and `Explain changes` have no contribution point (documented hook only). Not seen on a device |
| Explorer | in-progress | collapse all, VS Code menu order with key hints, status-tinted names, changed-folder tint, unsaved dot, F2 / Delete / Ctrl+C / X / V on a focused row. Not built: multi-select, drag and drop, Open With, Reveal in Files |
| **Hygiene** | | |
| ux-overhaul Pillar 4 superseded note | not-started | shell-model replaces its layout plan |
| ADR 0019 amendment note | not-started | accent and motif |

## R1 swap (done)

The temporary adapters (`LegacyAdapters.kt`, `PanelBinding.spansWindow`, `HomeScreen`, `SettingsScreen`, `ExtensionsScreen`) are gone. What the shell does now:

1. `AppRenderers` binds `home.projects`, `settings.categories` and `extensions.list` to their panels and `easyide.project`, `easyide.settings`, `easyide.extension` to their pages. A panel binding may carry a `stageDefault` shown on a wide window while no document is open: Home's is `HomeNowPage`, Settings' is the Appearance page.
2. `DocumentRenderer.Title` (composable) gives a tab or compact title bar the subject's own name (project name, extension name, settings category); the type's one-word title stays as the fallback, so a missing subject still has a title.
3. Dialogs that belong to no panel (`HomeDialogHost`, `ExtensionsDialogs`) are mounted once through `ShellHost(dialogs = ...)`.
4. Each panel draws its own title row (Home, Extensions, Settings); `PanelHost` adds none, so there is exactly one header.
5. Baseline profile: the generator taps the project row by `kit:project-row`, then the page's `kit:project-open` button; `ShellHost` exposes test tags as resource ids. Regeneration needs a device (`:app:generateBaselineProfile`) and has not been run.
6. Not done on purpose: extension contributions to the layout catalog (R4 feeds the registries; `LayoutCatalog.of` already names the pack), and duplicate/clone/import still select the new project in `HomeViewModel` without opening its page.
