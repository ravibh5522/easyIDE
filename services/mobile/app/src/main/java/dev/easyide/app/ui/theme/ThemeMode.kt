package dev.tabcode.app.ui.theme

/**
 * The curated theme set from docs/design-system/arch.md. Deliberately closed
 * (not an open custom-theme editor) for v1 - see that doc's "Themes" section.
 */
enum class ThemeMode {
    SYSTEM_DEFAULT,
    LIGHT,
    DARK,
    DYNAMIC,
    AMOLED_BLACK,
    HIGH_CONTRAST,
}
