[package]
name = "wasm-word-count"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
easyide-guest = { path = "../../../guest-rust" }

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
strip = true
